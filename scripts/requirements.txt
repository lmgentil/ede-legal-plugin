# Dependências de scripts/docx_template_engine.py e dos CLIs que o usam
# (render_docx.py, validate_template.py).
#
# Situação na Etapa 5.10 (ADR-0014, PEND-007) — dependência runtime do
# skill "docx" de terceiro REMOVIDA (Commit 3): docx_template_engine.py,
# docx_context_engine.py, docx_block_engine.py e validate_template.py
# usam exclusivamente o runtime próprio do EDE
# (scripts/docx_package.py — extrair_pacote_docx/empacotar_pacote_docx),
# sem unpack.py/pack.py/instalação separada de terceiro. PEND-007
# permanece aberta só pelo registro histórico (ADR-0014) até revisão
# formal de fechamento.
lxml

# lxml é usada por scripts/docx_package.py, docx_template_engine.py,
# docx_numeracao_engine.py e docx_block_engine.py — toda a manipulação
# XML do pipeline de Contestação, código próprio do EDE.
#
# defusedxml foi avaliada e REJEITADA nesta etapa como dependência de
# scripts/docx_package.py — decisão e justificativa completas no
# docstring desse módulo ("Decisão lxml × defusedxml"): lxml.etree com
# parser configurado para processamento seguro (resolve_entities=False,
# no_network=True, load_dtd=False) já cobre XXE/expansão de entidade sem
# adicionar uma segunda pilha de XML ao projeto. Não listar defusedxml
# aqui.

# validate_placeholders.py sozinho NÃO precisa de nada disto — só lê o zip
# do .docx com a stdlib (zipfile) para conferir placeholders.
