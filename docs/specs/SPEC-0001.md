# SPEC-0001 — ARQUITETURA V1 DO EDE LEGAL PLUGIN

**Status:** Draft
**Versão:** 0.1.0
**Tipo:** Especificação de Arquitetura e Produto
**Escopo:** Plugin jurídico para Claude Code
**Primeiro módulo processual:** Contestação
**Objetivo de expansão:** múltiplas peças processuais
**Repositório:** público no GitHub, ressalvados assets institucionais sensíveis

---

# 1. OBJETIVO

Esta especificação define os requisitos funcionais, arquiteturais, documentais e de qualidade da **Arquitetura v1 do EDE Legal Plugin**.

O sistema terá como finalidade auxiliar advogados na elaboração de peças processuais por meio do Claude Code, combinando:

* Skills jurídicas especializadas;
* modelos DOCX institucionais;
* RAG jurídico;
* embeddings vetoriais;
* busca híbrida;
* validação de citações;
* controle de integridade documental;
* cálculo de tempestividade;
* atualização versionada do plugin;
* arquitetura extensível para novos tipos de peças.

A primeira peça suportada será a **Contestação**.

A arquitetura não poderá, contudo, ser específica ou rigidamente acoplada à Contestação.

---

# 2. PRINCÍPIOS DA ARQUITETURA

## INV-001 — Modularidade

Cada tipo de peça processual deverá ser implementado como módulo próprio.

A inclusão de novas peças não deverá exigir reescrita dos componentes transversais.

## INV-002 — Integridade documental

O template institucional será considerado fonte estrutural autoritativa.

Nenhum conteúdo protegido poderá ser alterado fora das áreas expressamente autorizadas.

## INV-003 — Fonte jurídica verificável

Sempre que houver fundamento normativo, a redação deverá utilizar conhecimento jurídico recuperado de fonte verificável, preferencialmente por meio do RAG.

## INV-004 — Não invenção de conteúdo jurídico

O sistema não poderá inventar:

* leis;
* artigos;
* resoluções;
* súmulas;
* enunciados;
* precedentes;
* números de processos;
* teor de julgados;
* datas processuais;
* fatos do processo.

## INV-005 — Separação entre fatos e direito

Informações extraídas dos documentos do processo e conhecimento recuperado do RAG deverão permanecer logicamente separados.

## INV-006 — Fail Closed

Na ausência de evidência suficiente, o sistema deverá interromper ou sinalizar a insuficiência, em vez de preencher lacunas por inferência não verificável.

## INV-007 — Extensibilidade

Contestação deverá ser o primeiro módulo, e não o centro da arquitetura.

---

# 3. VISÃO GERAL DO SISTEMA

A arquitetura deverá seguir, conceitualmente:

```text
EDE Legal Plugin
│
├── Core
│
├── Skills Processuais
│   └── Contestação
│
├── Skills Transversais
│   ├── redator-peca-processual-elite
│   ├── humanizer-pt-br
│   └── calendario-forense-tjba-2026
│
├── Templates
│   └── Contestação
│
├── RAG Jurídico
│   ├── Corpus
│   ├── Embeddings
│   ├── Busca Lexical
│   ├── Busca Vetorial
│   ├── Reranking
│   └── Metadados
│
├── Validação Jurídica
│
├── Template Lock
│
├── Renderização DOCX
│
├── Atualização
│
└── Testes
```

---

# 4. ESTRUTURA DE DIRETÓRIOS

## REQ-001 — Estrutura modular

A estrutura inicial deverá seguir, preferencialmente:

```text
ede-legal-plugin/
│
├── .claude-plugin/
│   └── plugin.json
│
├── skills/
│   ├── contestacao/
│   │   └── SKILL.md
│   │
│   ├── redator-peca-processual-elite/
│   │   └── SKILL.md
│   │
│   ├── humanizer-pt-br/
│   │   └── SKILL.md
│   │
│   └── calendario-forense-tjba-2026/
│       └── SKILL.md
│
├── templates/
│   └── contestacao/
│       ├── modelo-oficial.docx
│       └── schema.json
│
├── rag/
│   ├── corpus/
│   │   ├── legislacao/
│   │   ├── regulamentos/
│   │   ├── enunciados/
│   │   └── jurisprudencia/
│   │
│   ├── index/
│   ├── metadata/
│   ├── scripts/
│   └── config.yaml
│
├── commands/
│   ├── contestacao.md
│   ├── updateEde.md
│   └── atualizar-rag.md
│
├── scripts/
│   ├── render_docx.py
│   ├── validate_template.py
│   ├── validate_placeholders.py
│   └── update_plugin.py
│
├── tests/
│
├── docs/
│   ├── specs/
│   └── adr/
│
├── VERSION
├── CHANGELOG.md
├── README.md
├── LICENSE
├── .gitignore
└── .env.example
```

A estrutura poderá sofrer alteração se houver justificativa arquitetural documentada.

---

# 5. SKILLS

## REQ-002 — Skill Contestação

Deverá existir:

```text
skills/contestacao/SKILL.md
```

Sua responsabilidade será exclusivamente orquestrar o fluxo da Contestação.

A Skill deverá:

1. identificar os documentos processuais relevantes;
2. extrair dados estruturados, com proveniência factual (REQ-030);
3. identificar fatos alegados;
4. acionar `estrategista-contestacao-ede` para a análise estratégica
   (estrutura defensiva, matriz de impugnação, teses, riscos) — ver
   REQ-002-A/INV-CONTESTACAO-ESTRATEGIA;
5. identificar pedidos;
6. identificar datas relevantes;
7. determinar necessidade de análise de tempestividade;
8. identificar questões jurídicas;
9. consultar o RAG quando necessário e validar juridicamente o que for
   recuperado (Fase 5);
10. acionar as Skills transversais obrigatórias (`redator-peca-processual-elite`,
    `humanizer-pt-br`, e `calendario-forense-tjba-2026` quando aplicável);
11. gerar o conteúdo dos placeholders;
12. enviar o conteúdo ao motor de renderização;
13. validar o documento final.

A Skill `contestacao` **orquestra** esse fluxo — não duplica internamente
o que `estrategista-contestacao-ede`, `redator-peca-processual-elite` ou
`humanizer-pt-br` já fazem; ela aciona cada uma e consome sua saída.

## REQ-002-A — Dependência obrigatória do estrategista (Contestação)

Toda elaboração de Contestação deverá acionar, antes da etapa de redação:

```text
estrategista-contestacao-ede
```

### INV-CONTESTACAO-ESTRATEGIA

> Toda elaboração de Contestação deverá obrigatoriamente executar a Skill
> `estrategista-contestacao-ede` antes da etapa de redação. A Skill
> `contestacao` atua como orquestradora e não poderá suprimir, substituir
> silenciosamente ou tratar como opcional a etapa estratégica.

A ausência ou falha da Skill `estrategista-contestacao-ede` impede que o
pipeline considere a etapa estratégica concluída — nenhum fallback
silencioso que a ignore é permitido (CLAUDE.md §17, Fail Closed).

## REQ-003 — Dependência obrigatória do redator

Toda elaboração de Contestação deverá acionar:

```text
redator-peca-processual-elite
```

## REQ-004 — Dependência obrigatória do humanizer

Toda elaboração de Contestação deverá acionar:

```text
humanizer-pt-br
```

A Skill de humanização não poderá alterar o sentido jurídico da peça.

## REQ-005 — Tempestividade

Toda análise de tempestividade relativa ao TJBA no ano de 2026 deverá acionar:

```text
calendario-forense-tjba-2026
```

### INV-TEMPESTIVIDADE-MARCO

> Nenhuma Contestação poderá alcançar a etapa de redação final/geração do
> template sem marco temporal (data de citação, intimação ou publicação)
> concreto — documentalmente identificado com proveniência, ou
> expressamente informado pelo advogado em resposta a pergunta direta da
> Skill `contestacao`. `PENDENTE DE VALIDAÇÃO` (REQ-012) deixa de ser
> estado terminal aceitável para o placeholder `TEMPESTIVIDADE_CASO` —
> não existe opção de prosseguir "sob responsabilidade do advogado" sem
> marco concreto.

A Skill `contestacao` não poderá:

* presumir ou inventar o marco temporal;
* concluir tempestividade sem marco concreto;
* deixar `PENDENTE DE VALIDAÇÃO` (ou equivalente) alcançar o documento
  final;
* perguntar novamente ao advogado quando o marco já estiver
  inequivocamente documentado com proveniência.

Se o marco estiver ausente, ambíguo, conflitante ou insuficientemente
comprovado nos documentos do caso, a Skill deverá perguntar
obrigatoriamente ao advogado antes de acionar a redação:

> "Qual foi a data da citação, intimação ou publicação relevante para a
> contagem do prazo?"

Enquanto não houver marco temporal concreto — documentado ou informado —
o pipeline deverá permanecer bloqueado (`PIPELINE_ABORTED`,
`stage=tempestividade`). `calendario-forense-tjba-2026` permanece o
único mecanismo de cálculo (REQ-005/011/012) — esta invariante não
introduz cálculo alternativo, apenas condiciona quando o resultado do
cálculo pode ser aceito como definitivo.

## REQ-002-B — Composição de blocos condicionais (Etapa 5)

O template institucional marca estruturalmente, via `<w:sdt>`, as teses
condicionais da Contestação (preliminares, dever legal de fiscalização,
desnecessidade de aviso prévio, cálculos de recuperação de consumo,
evolução de consumo, licitude do corte/suspensão, nexo causal
indemonstrado, descabimento de dano moral, Reconvenção — e o inline do
título vinculado a ela). O catálogo estrutural formal —
`templates/contestacao/blocos.json` — é a fonte única de verdade da
hierarquia (id, tag, tipo, parent/children, decision_mode, placeholders,
dependencies, cardinality) consumida por `scripts/docx_block_engine.py`.

Quatro tipos de bloco: `FIXO` (nunca removível — não tem SDT, é texto
institucional do template), `CONDICIONAL_PADRAO` (texto institucional
pronto; a etapa estratégica só decide incluir/excluir — ex.:
`EVOLUCAO_CONSUMO`, desde a remoção definitiva de
`ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA`), `CONDICIONAL_HIBRIDO`
(texto institucional + placeholder(s) interno(s) preenchidos por caso —
ex.: `RECONVENCAO`/`VALOR_FRA`), `CONTAINER_DERIVED` (não recebe decisão direta
— seu estado deriva declarativamente dos filhos via `derived_rule` do
catálogo, ex.: `PRELIMINARES`/`ANY_CHILD_INCLUDED`).

### INV-COMPOSICAO-BLOCOS

> A inclusão ou exclusão de teses condicionais da Contestação será
> decidida exclusivamente pela etapa estratégica obrigatória
> (`estrategista-contestacao-ede`, via Skill `contestacao`). O motor
> documental (`scripts/docx_block_engine.py`) limitar-se-á à validação
> estrutural do catálogo e à execução determinística dessas decisões —
> nunca conterá heurística jurídica própria (nenhum
> `if consumidor: incluir_cdc()`). Nenhuma tese `INDETERMINADA` poderá
> alcançar o DOCX final.

Consequências obrigatórias:

* estado de bloco é exatamente um de `INCLUIR`/`EXCLUIR`/`INDETERMINADO`
  — nunca normalizado a partir de booleano, número ou outra string;
* `INDETERMINADO` em qualquer folha aborta o pipeline
  (`PIPELINE_ABORTED`, `stage=decisao_indeterminada`), mesmo que blocos
  irmãos já estejam decididos — nenhuma inferência jurídica parcial;
* blocos `CONTAINER_DERIVED` e o inline vinculado (`decision_mode:
  "linked"`) nunca recebem decisão manual — resolvidos automaticamente
  pelo motor a partir do catálogo (`stage=decisao_invalida` se receberem);
* o catálogo é validado estruturalmente antes de qualquer decisão ser
  processada (ids/tags únicos, parent/children existentes e coerentes,
  sem ciclos, tipo/decision_mode/cardinalidade consistentes,
  dependencies referenciando placeholders declarados) —
  `stage=catalogo_invalido` em qualquer inconsistência;
* todo `<w:sdt>` do template é cruzado contra o catálogo antes da
  composição — tag desconhecida, tag do catálogo ausente no template, SDT
  sem `w:tag`/`w:sdtContent`, ou cardinalidade divergente (duplicata não
  autorizada) abortam antes de qualquer mutação estrutural;
* a composição (`INCLUIR` = unwrap preservando parágrafos/runs/drawings/
  bookmarks/`mc:AlternateContent` originais; `EXCLUIR` = remove o `<w:sdt>`
  inteiro) nunca assume índice de parágrafo igual a índice de filho do
  corpo do documento — o template real intercala elementos não-`w:p`
  (`<w:bookmarkEnd>`) entre parágrafos;
* o Template Lock (REQ-016/TEST-001) se estende — sem enfraquecer — para
  cobrir a composição: o "esperado" recomputado passa a ser
  template → composição de blocos → renumeração dinâmica dos títulos
  (INV-NUMERACAO-DINAMICA-CONTESTACAO, §55) → substituição de
  placeholders, comparado byte a byte contra o gerado;
* nenhuma etapa desta composição produz DOCX parcial: qualquer falha
  interrompe antes da escrita do arquivo final.

## REQ-002-C — Entrega final da Contestação (Etapa 5.6)

Quando a intenção do pedido for elaborar, gerar, preparar ou produzir a
Contestação (`MODO PRODUÇÃO`), a Skill `contestacao` deverá conduzir o
pipeline completo (REQ-002) numa única execução contínua e entregar ao
advogado a **Contestação final em DOCX** — nunca um artefato intermediário
do pipeline como resposta de encerramento.

### INV-CONTESTACAO-ENTREGA-DOCX

> Análise estratégica, extração factual, RAG, decisões automáticas de
> blocos condicionais, redação, humanização, validações e composição são
> etapas internas do mesmo fluxo de produção da Contestação — nenhuma
> delas constitui resposta final ao advogado. A conclusão de qualquer
> etapa interna não poderá encerrar a execução, nem gerar pedido de
> autorização para prosseguir à etapa seguinte, quando o pedido original
> já for de elaboração da peça.

A Skill `contestacao` só poderá interromper a execução antes do DOCX
final por um destes dois motivos:

1. **Decisão humana obrigatória pendente** — Reconvenção
   (REQ-002-A/`INV-RECONVENCAO-AUTORIZACAO-EXPRESSA`) ou licitude de
   corte/suspensão com fato de corte efetivo comprovado
   (`INV-CORTE-GATE-HUMANO`) sem decisão ainda registrada. A pergunta
   deverá ser restrita à(s) decisão(ões) efetivamente pendente(s) — nunca
   ambas por hábito quando só uma está em aberto — e nunca repetida
   quando o advogado já a tiver respondido no próprio pedido inicial.
2. **Fail-closed técnico/fático real** já previsto neste documento —
   bloco condicional `INDETERMINADO`, tempestividade sem marco temporal
   (`INV-TEMPESTIVIDADE-MARCO`), `JUIZO` não resolvível via DataJud/CNJ
   (`INV-JUIZO-DATAJUD`), valor divergente não resolvível documentalmente,
   ou documento indispensável ilegível.

Em ambos os casos, a Skill deverá perguntar somente o dado necessário
para destravar o pipeline e, recebida a resposta, **retomar
automaticamente a execução até o DOCX final** — nunca voltar a entregar
a análise estratégica ou qualquer outro artefato interno como se fosse a
resposta solicitada.

Pedidos que não solicitam a peça pronta (`MODO ANÁLISE` — "analise este
processo", "faça só a estratégia"; `MODO CONSULTIVO` — "quais teses cabem
nesse caso?") permanecem fora do escopo desta invariante: a análise ou a
resposta pontual É a entrega correta nesses modos, e a Skill
`estrategista-contestacao-ede`, quando acionada diretamente pelo usuário
(fora da orquestração de `contestacao`), continua entregando sua análise
como artefato final, com sugestão explícita de próximo passo — ver
`skills/estrategista-contestacao-ede/SKILL.md` §9.

---

# 6. SKILL REDATOR-PECA-PROCESSUAL-ELITE

## REQ-006 — Padrão redacional

A Skill deverá impor padrão técnico de redação jurídica com:

* linguagem profissional;
* objetividade;
* coerência argumentativa;
* encadeamento entre fato, norma e conclusão;
* ausência de redundância desnecessária;
* precisão terminológica;
* formulação adequada de pedidos.

## REQ-007 — Proibição de invenção factual

A Skill não poderá criar fatos que não estejam:

* nos documentos do processo;
* em informação expressamente fornecida pelo advogado.

## REQ-008 — Proibição de invenção jurídica

A Skill não poderá criar fundamentos jurídicos inexistentes.

---

# 7. SKILL HUMANIZER-PT-BR

## REQ-009 — Função

A Skill deverá revisar a linguagem produzida pelo redator para remover sinais artificiais de geração automática.

## REQ-010 — Limites

A Skill não poderá modificar:

* fundamentos;
* fatos;
* pedidos;
* valores;
* citações;
* conclusão jurídica.

Poderá modificar:

* ritmo;
* conectivos;
* fluidez;
* repetição;
* construções artificiais;
* linguagem excessivamente mecânica.

---

# 8. SKILL CALENDARIO-FORENSE-TJBA-2026

## REQ-011 — Cálculo auditável

A análise de tempestividade deverá produzir memória de cálculo.

Deverá registrar, quando aplicável:

* data da publicação;
* data da ciência;
* termo inicial;
* prazo legal;
* dias úteis;
* feriados;
* suspensões;
* termo final;
* fundamento normativo utilizado.

## REQ-012 — Ausência de dado essencial

Se faltar dado essencial ao cálculo, o sistema deverá marcar a tempestividade como:

```text
PENDENTE DE VALIDAÇÃO
```

e não deverá presumir datas.

---

# 9. TEMPLATE DE CONTESTAÇÃO

## REQ-013 — Template oficial

O arquivo:

```text
templates/contestacao/modelo-oficial.docx
```

será considerado modelo institucional protegido.

## INV-008 — Conteúdo protegido

Todo conteúdo que não seja:

1. placeholder autorizado; ou
2. seção condicional expressamente mapeada

deverá permanecer intocado.

---

# 10. PLACEHOLDERS AUTORIZADOS

## REQ-014 — Placeholders

Lista extraída por auditoria direta de `templates/contestacao/modelo-oficial.docx`
(Fase 3, 2026-08-18) — não é mais a lista hipotética original desta SPEC.
`TIPO_ACAO`, `TITULO_TESE_FATICA` e `VALOR_DANO_MORAL` foram removidos por
não existirem no template real (o tipo de ação e a tese central estão fixos
no texto; não há pedido de dano moral neste modelo, que é de Contestação
com Reconvenção por cobrança de fraude/irregularidade em medição de
energia). `FOTOS_DA_IRREGULARIADE` é grafado exatamente como está no DOCX
(erro de digitação no próprio arquivo — falta um "D"); a engine precisa
casar com o token real, não com a grafia correta.

```text
{{JUIZO}}
{{NUMERO_PROCESSO}}
{{AUTOR}}
{{TEMPESTIVIDADE_CASO}}
{{SINOPSE_FATOS}}
{{REALIDADE_FATICA}}
{{IRREGULARIDADE_ENCONTRADA}}
{{DESENVOLVIMENTO_TECNICO_IRREGULARIDADE}}
{{FOTOS_DA_IRREGULARIADE}}
{{VALOR_FRA}}
{{VALOR_DANO_MORAL_PRETENDIDO}}
{{PEDIDOS_FINAIS}}
{{LOCAL_DATA}}
```

`ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA` foi removido definitivamente
desta lista (correção pontual posterior) — o modelo oficial passou a
trazer a argumentação de evolução de consumo inteiramente fixa, sem
marcador; ver §49.

`{{VALOR_DANO_MORAL_PRETENDIDO}}` foi inserido manualmente pelo advogado
no modelo oficial (correção pontual posterior à §49, 12→13
placeholders), dentro do bloco `DESCABIMENTO_DANO_MORAL` — homônimo do
`VALOR_DANO_MORAL` acima (removido por não existir no template
auditado na Fase 3) mas semanticamente distinto: aquele seria um
placeholder de PEDIDO de dano moral (que este modelo, de Contestação,
nunca formula); este é o valor que a AUTORA pediu, citado
defensivamente no tópico que argumenta pelo descabimento do dano moral.
Ver §53.

## INV-009 — Alteração restrita

Nenhum conteúdo fora desses campos poderá ser modificado automaticamente sem alteração explícita do schema e da especificação.

---

# 11. SCHEMA DO TEMPLATE

## REQ-015 — Schema formal

Deverá existir:

```text
templates/contestacao/schema.json
```

Contendo, no mínimo:

```json
{
  "template": "modelo-oficial.docx",
  "version": "1.0.0",
  "strict_template": true,
  "allow_unmapped_changes": false,
  "editable_placeholders": [
    "JUIZO",
    "NUMERO_PROCESSO",
    "AUTOR",
    "TEMPESTIVIDADE_CASO",
    "SINOPSE_FATOS",
    "REALIDADE_FATICA",
    "IRREGULARIDADE_ENCONTRADA",
    "DESENVOLVIMENTO_TECNICO_IRREGULARIDADE",
    "FOTOS_DA_IRREGULARIADE",
    "VALOR_FRA",
    "VALOR_DANO_MORAL_PRETENDIDO",
    "PEDIDOS_FINAIS",
    "LOCAL_DATA"
  ]
}
```

---

# 12. TEMPLATE LOCK

## REQ-016 — Proteção estrutural

Deverá existir mecanismo denominado **Template Lock**.

Ele deverá comparar o template-fonte e o documento gerado, desconsiderando somente as áreas expressamente mutáveis.

## TEST-001 — Alteração indevida

O teste deverá falhar se qualquer área protegida for:

* alterada;
* removida;
* adicionada;
* reordenada indevidamente.

## TEST-002 — Preservação visual

Deverão ser preservados:

* cabeçalho;
* rodapé;
* timbrado;
* tabelas;
* fontes;
* estilos;
* margens;
* espaçamento;
* imagens;
* elementos institucionais.

---

# 13. RAG JURÍDICO

## REQ-017 — Camada independente

O RAG deverá ser implementado como componente independente das Skills.

## REQ-018 — Tipos de conteúdo

O corpus deverá comportar:

```text
legislação
regulamentos
enunciados
jurisprudência
```

## INV-010 — Corpus fora das Skills

Legislação integral não deverá ser inserida diretamente em arquivos `SKILL.md`.

---

# 14. EMBEDDINGS

## REQ-019 — Embeddings vetoriais

O RAG deverá utilizar embeddings para recuperação semântica.

## REQ-020 — Chunking jurídico

O chunking deverá respeitar, sempre que possível, a estrutura normativa.

Exemplo:

```text
Diploma
→ artigo
→ parágrafo
→ inciso
→ alínea
```

Chunks não deverão ser divididos de maneira que destrua o sentido jurídico.

---

# 15. BUSCA HÍBRIDA

## REQ-021 — Busca vetorial

O sistema deverá possuir busca semântica por embeddings.

## REQ-022 — Busca lexical

O sistema deverá possuir mecanismo lexical para referências exatas.

## REQ-023 — Fusão

Os resultados deverão ser combinados antes da entrega ao LLM.

Fluxo conceitual:

```text
consulta
  ↓
normalização
  ↓
busca lexical + busca vetorial
  ↓
fusão
  ↓
reranking
  ↓
contexto jurídico
```

---

# 16. METADADOS JURÍDICOS

## REQ-024 — Metadados mínimos

Cada unidade indexada deverá armazenar, quando aplicável:

```text
id
tipo
órgão
tribunal
diploma
norma
artigo
parágrafo
inciso
alínea
tema
texto
fonte
URL
data de publicação
data de verificação
vigência
jurisdição
hierarquia
```

---

# 17. AUTORIDADE DE FONTES

## REQ-025 — Priorização

A recuperação deverá privilegiar fontes oficiais.

Ordem conceitual:

```text
fonte oficial
>
repositório institucional
>
fonte secundária confiável
>
fonte meramente informativa
```

---

# 18. VIGÊNCIA

## REQ-026 — Estado temporal

Conteúdo jurídico deverá poder possuir estado:

```text
vigente
alterado
revogado
suspenso
histórico
não verificado
```

## INV-011 — Norma revogada

Norma revogada não poderá ser apresentada como vigente.

---

# 19. VALIDAÇÃO DE CITAÇÕES

## REQ-027 — Verificação obrigatória

Toda citação normativa ou jurisprudencial inserida na peça deverá possuir correspondência em fonte recuperada ou validada.

## REQ-028 — Citação não encontrada

Caso não seja possível validar uma citação:

1. realizar nova consulta;
2. procurar fonte oficial;
3. se não encontrada, remover ou marcar como não validada.

Nunca inventar conteúdo para completar a citação.

---

# 20. RASTREABILIDADE

## REQ-029 — Proveniência jurídica

Cada fundamento utilizado deverá possuir internamente vínculo com sua fonte.

## REQ-030 — Proveniência factual

Sempre que possível, fatos deverão armazenar referência ao documento de origem.

Exemplo:

```json
{
  "fact": "A linha foi cancelada em 12/11/2025.",
  "source_document": "documento-x.pdf",
  "page": 4,
  "confidence": 0.99
}
```

---

# 21. PIPELINE DA CONTESTAÇÃO

## REQ-031 — Fluxo mínimo

O pipeline deverá seguir:

```text
documentos
  ↓
extração
  ↓
classificação dos dados
  ↓
fatos
  ↓
pedidos
  ↓
datas
  ↓
tempestividade
  ↓
questões jurídicas
  ↓
RAG
  ↓
validação das fontes
  ↓
redator-peca-processual-elite
  ↓
humanizer-pt-br
  ↓
validação factual e jurídica
  ↓
preenchimento de placeholders
  ↓
Template Lock
  ↓
DOCX final
```

---

# 22. RENDERIZAÇÃO DOCX

## REQ-032 — Renderizador

Deverá existir:

```text
scripts/render_docx.py
```

## INV-012 — Template como base

O documento final deverá ser gerado a partir de cópia do template original.

O motor não deverá reconstruir o DOCX do zero.

## REQ-033 — Preservação OOXML

A implementação deverá preservar, sempre que tecnicamente aplicável:

* runs;
* estilos;
* tabelas;
* cabeçalhos;
* rodapés;
* imagens;
* hyperlinks;
* propriedades do documento.

---

# 23. VALIDAÇÃO DE PLACEHOLDERS

## REQ-034 — Validador

Deverá existir:

```text
scripts/validate_placeholders.py
```

## TEST-003 — Placeholder desconhecido

A geração deverá falhar se houver placeholder não reconhecido.

## TEST-004 — Placeholder residual

O documento final não deverá conter placeholders não processados.

---

# 24. RELATÓRIO FINAL

## REQ-035 — Relatório operacional

Após cada geração deverá existir relatório separado da peça.

Exemplo:

```text
Peça: Contestação
Template: 1.0.0
Template Lock: OK
Placeholders: OK
Tempestividade: Validada
RAG: Consultado
Fontes recuperadas: 8
Citações validadas: 5
Citações pendentes: 0
Documento final: ...
```

---

# 25. COMANDOS

## REQ-036 — Contestação

Deverá existir comando para iniciar o fluxo de Contestação.

A implementação deverá utilizar a forma oficialmente suportada pelo Claude Code na versão vigente.

## REQ-037 — Atualização

Deverá existir comando ou mecanismo equivalente a:

```text
/updateEde
```

## REQ-038 — Atualização do RAG

Deverá existir comando ou mecanismo equivalente a:

```text
/atualizar-rag
```

---

# 26. UPDATEEDE

## REQ-039 — Comportamento

O fluxo deverá:

1. identificar versão local;
2. identificar versão disponível;
3. comparar versões;
4. atualizar somente se necessário;
5. preservar configurações locais;
6. executar validações pós-atualização;
7. informar resultado;
8. apresentar resumo do CHANGELOG.

## INV-013 — Configuração local

Atualização não poderá apagar silenciosamente dados locais.

---

# 27. VERSIONAMENTO

## REQ-040 — SemVer

O projeto utilizará versionamento semântico:

```text
MAJOR.MINOR.PATCH
```

## REQ-041 — Arquivos

Deverão existir:

```text
VERSION
CHANGELOG.md
```

---

# 28. SEGURANÇA

## SEC-001 — Repositório público

Nenhuma credencial poderá ser incluída no repositório público.

## SEC-002 — Dados proibidos

Nunca versionar:

* API keys;
* tokens;
* senhas;
* `.env` real;
* dados sigilosos;
* documentos processuais reais;
* dados pessoais desnecessários;
* credenciais institucionais.

## SEC-003 — Workspace local

Arquivos dos processos deverão permanecer em diretório local ignorado pelo Git.

---

# 29. ASSETS INSTITUCIONAIS

## SEC-004 — Timbrado

O projeto deverá considerar que o modelo oficial do escritório pode ser ativo institucional não destinado à exposição pública.

## ADR-REQ-001

Deverá ser documentada decisão específica sobre:

```text
core público
+
assets institucionais públicos ou privados
```

Não assumir automaticamente que o timbrado deverá integrar o GitHub público.

## REQ-046 — Repositório e plugin públicos, template externo (decisão definitiva)

Decisão registrada (ADR-0009), não mais em aberto: o repositório e o
plugin **serão públicos**. O template institucional real
(`templates/contestacao/modelo-oficial.docx`) **nunca integra o
repositório nem o pacote distribuído pelo plugin** — é fornecido
externamente, fora deste repositório, aos usuários autorizados a
recebê-lo. `templates/contestacao/schema.json` (o contrato de
placeholders) permanece público.

Acesso técnico ao repositório (visualizar, clonar, baixar, instalar via
marketplace) não equivale a autorização jurídica irrestrita sobre o
software — os direitos de uso, modificação, redistribuição e exploração
são disciplinados pela `LICENSE`.

## REQ-047 — Ausência do template não invalida a instalação

Uma instalação pública do plugin nunca terá o template institucional
real presente por padrão — isso é comportamento esperado, não falha. O
sistema não poderá tratar essa ausência como instalação corrompida.

### INV-015 — Fail closed restrito à etapa que depende do template

Quando a geração do DOCX final for solicitada sem o template presente, o
pipeline deverá interromper especificamente a etapa que dele depende —
preservando como concluídas as etapas anteriores que não dependem dele
(extração factual, tempestividade, estratégia, RAG, validação jurídica,
redação, humanização). O sistema não poderá inventar, baixar
automaticamente ou reconstruir um template substituto.

---

# 30. TESTES

## TEST-005 — Skills

Deverá haver teste ou validação de que Contestação exige:

```text
estrategista-contestacao-ede
redator-peca-processual-elite
humanizer-pt-br
```

`estrategista-contestacao-ede` é obrigatória e antecede a redação
(INV-CONTESTACAO-ESTRATEGIA) — o teste deve falhar se
`skills/contestacao/SKILL.md` deixar de declará-la como dependência
obrigatória, não apenas recomendada.

## TEST-006 — Calendário

Deverá ser validado o acionamento do calendário quando houver análise de tempestividade TJBA/2026.

## TEST-007 — RAG

Testar:

* ingestão;
* chunking;
* indexação;
* busca lexical;
* busca vetorial;
* fusão;
* metadados;
* reranking.

## TEST-008 — Citações

Uma citação inexistente deverá ser recusada ou sinalizada.

## TEST-009 — DOCX

Documento final deverá abrir sem corrupção.

## TEST-010 — Template Lock

Qualquer modificação não autorizada deverá resultar em falha.

## TEST-011 — Tempestividade sem marco (INV-TEMPESTIVIDADE-MARCO)

Tempestividade aplicável, sem marco temporal documentado com proveniência
nem informado pelo advogado, deverá resultar em `PIPELINE_ABORTED`,
`stage=tempestividade`, sem produzir documento final. Marco documentado
com proveniência válida, ou marco informado pelo advogado com resposta
registrada, deverá prosseguir normalmente sem repetir a pergunta.

---

# 31. LOGS

## REQ-042 — Logging

O sistema deverá gerar logs técnicos suficientes para diagnóstico.

Exemplo:

```text
[INFO] Template carregado
[INFO] Placeholders validados
[INFO] RAG consultado
[INFO] 7 resultados recuperados
[INFO] Citações verificadas
[INFO] Template Lock aprovado
[INFO] Documento gerado
```

## SEC-005 — Logs

Logs não deverão expor credenciais ou dados pessoais desnecessários.

---

# 32. EXTENSIBILIDADE

## REQ-043 — Nova peça

A arquitetura deverá permitir que uma nova peça seja adicionada primordialmente por:

```text
skills/<nova-peca>/SKILL.md

templates/<nova-peca>/
├── modelo-oficial.docx
└── schema.json
```

## INV-014 — Componentes compartilhados

Novas peças deverão reutilizar:

* redator;
* humanizer;
* RAG;
* renderizador;
* Template Lock;
* validação;
* updater.

## INV-GATE-CONTESTACAO — Trava de validação humana antes da expansão

> A Contestação é a única peça processual autorizada para desenvolvimento,
> estabilização, correção e testes no estágio atual do EDE Legal Plugin.
> É vedado iniciar, projetar, implementar, antecipar, criar scaffolding,
> criar templates, criar Skills, criar estrategistas, criar catálogos de
> blocos ou realizar refatorações destinadas especificamente à inclusão
> de Recurso Inominado, Embargos, manifestações ou qualquer outra nova
> peça processual enquanto a Contestação não tiver sido validada
> integralmente pelo usuário em utilização real. Testes automatizados,
> cobertura E2E, validação estrutural, aprovação técnica ou declaração de
> estabilidade pelo agente não substituem a validação humana do usuário.
> A liberação para expansão do projeto dependerá de manifestação expressa
> do usuário após sua validação prática da Contestação.

Registrada após a Etapa 5 (Motor Composicional de Blocos Condicionais):
a suíte técnica (141/141 testes, RAG top-1 75%/top-3 88%, `claude plugin
validate .` aprovado) demonstra apenas validação técnica — nunca
validação profissional, jurídica final, visual final, operacional, em
casos reais, ou aprovação da Contestação pelo usuário. Nenhum agente,
teste, script, CI, relatório ou critério automático pode substituir essa
manifestação; a formulação correta enquanto esta invariante está ativa é
"Contestação tecnicamente apta para testes reais — não validada pelo
usuário", nunca "validada"/"concluída definitivamente"/"pronta para
produção"/"100% aprovada".

Enquanto ativa:

* **permanece autorizado**: testar, corrigir, melhorar e documentar a
  Contestação existente (estratégia, DOCX, SDTs/blocos, placeholders,
  Template Lock, paginação, validação jurídica, proveniência,
  tempestividade), e criar testes de regressão decorrentes de bugs reais
  encontrados na validação prática;
* **permanece vedado**: iniciar qualquer nova peça processual (Skill,
  template, catálogo de blocos, schema, fixture, pipeline) — inclusive
  Recurso Inominado e Embargos — e generalizar prematuramente a
  arquitetura para acomodar peças futuras ainda não autorizadas (nenhum
  `piece_registry`/`piece_factory`/`base_piece` ou equivalente só porque
  "poderá ser útil depois"); a abstração multipeça só se justifica quando
  necessária para corrigir a própria Contestação, ou após o usuário
  liberar expressamente a expansão (REQ-043/INV-014 continuam vigentes
  como requisito arquitetural — apenas sua execução fica suspensa até a
  liberação).

A trava é liberada exclusivamente por manifestação expressa do usuário
("CONTESTAÇÃO VALIDADA" ou equivalente inequívoco) após testes reais —
nunca inferida de suíte verde, cobertura, CI ou relatório do agente.

---

# 33. CONFIGURAÇÃO

## REQ-044 — Configuração centralizada

Parâmetros mutáveis deverão ser centralizados.

Exemplos:

```text
modelo de embeddings
top_k
threshold
pesos do ranking
diretórios
versão do corpus
logging
```

Evitar valores mágicos espalhados pelo código.

---

# 34. DEPENDÊNCIAS

## REQ-045 — Controle de dependências

Toda nova dependência deverá ser avaliada quanto a:

* necessidade;
* manutenção;
* licença;
* segurança;
* estabilidade;
* impacto no projeto.

---

# 35. ADRs OBRIGATÓRIOS

Deverão ser criados ADRs para decisões arquiteturais relevantes.

Estrutura inicial recomendada:

```text
docs/adr/
├── ADR-0001-arquitetura-modular-das-pecas.md
├── ADR-0002-template-lock.md
├── ADR-0003-rag-juridico.md
├── ADR-0004-separacao-rag-skills.md
├── ADR-0005-estrategia-de-atualizacao.md
└── ADR-0006-assets-institucionais.md
```

Cada ADR deverá conter:

```text
Status
Contexto
Decisão
Alternativas consideradas
Consequências
```

---

# 36. FASES DE IMPLEMENTAÇÃO

## FASE 0 — Auditoria

Objetivo:

mapear o estado real do repositório antes de qualquer alteração.

Deverá identificar:

* estrutura atual;
* Skills existentes;
* templates;
* scripts;
* RAG existente;
* dependências;
* testes;
* código reutilizável.

### Saída obrigatória

Classificar requisitos como:

```text
[PRONTO]
[PARCIAL]
[AUSENTE]
[BLOQUEADO]
```

Nenhuma alteração deverá ser feita nessa fase.

---

## FASE 1 — Fundação

Implementar:

* manifest;
* estrutura;
* versão;
* changelog;
* configuração;
* documentação;
* segurança básica.

---

## FASE 2 — Skills transversais

Implementar ou integrar:

```text
redator-peca-processual-elite
humanizer-pt-br
calendario-forense-tjba-2026
```

---

## FASE 3 — Template Engine

Implementar:

* schema;
* placeholders;
* renderização;
* Template Lock;
* testes DOCX.

---

## FASE 4 — RAG Jurídico

Implementar:

* ingestão;
* normalização;
* chunking;
* embeddings;
* indexação;
* busca lexical;
* busca vetorial;
* fusão;
* metadados;
* reranking.

---

## FASE 5 — Validação Jurídica

Implementar:

* validação de citações;
* autoridade de fontes;
* vigência;
* rastreabilidade.

---

## FASE 6 — Contestação

Implementar e integrar:

```text
skills/contestacao/
```

---

## FASE 7 — End-to-End

Executar fluxo completo de geração da peça.

**Gate de entrada:** nenhum gate pendente. `PEND-001` (`docs/PENDENCIAS.md`)
— suporte multimídia ao placeholder `FOTOS_DA_IRREGULARIADE` — bloqueava
esta fase desde o encerramento da Fase 3 (2026-08-18); reclassificada como
`DEFERRED` na correção arquitetural v0.6.1 (2026-08-18): a V1 não
automatiza inserção de imagem — o campo recebe um marcador textual de
pós-edição manual pelo advogado, dentro do comportamento já existente do
Template Engine desde a Fase 3. Não bloqueia mais o início desta fase.

---

## FASE 8 — Atualização

Implementar:

```text
/updateEde
/atualizar-rag
```

ou equivalentes oficialmente suportados.

---

## FASE 9 — Hardening

Executar:

* testes;
* lint;
* segurança;
* instalação limpa;
* atualização;
* validação do template;
* regressão.

---

# 37. CRITÉRIOS DE ACEITE DA V1

A v1 somente poderá ser considerada concluída se:

* [ ] o plugin instalar corretamente;
* [ ] Contestação estiver operacional;
* [ ] estrategista-contestacao-ede for obrigatório (INV-CONTESTACAO-ESTRATEGIA);
* [ ] redator for obrigatório;
* [ ] humanizer for obrigatório;
* [ ] calendário for acionado quando aplicável;
* [ ] RAG estiver operacional;
* [ ] embeddings funcionarem;
* [ ] busca lexical funcionar;
* [ ] busca híbrida funcionar;
* [ ] fontes possuírem metadados;
* [ ] citações forem verificáveis;
* [ ] citações inexistentes forem rejeitadas;
* [ ] os 11 placeholders forem suportados;
* [ ] o template permanecer protegido;
* [ ] Template Lock estiver operacional;
* [ ] DOCX final não estiver corrompido;
* [ ] testes automatizados passarem;
* [ ] documentação estiver disponível;
* [ ] atualização estiver funcional;
* [ ] arquitetura estiver preparada para novas peças.

---

# 38. NÃO OBJETIVOS DA V1

A v1 não terá como objetivo implementar imediatamente:

* recurso inominado;
* embargos;
* agravos;
* manifestações;
* pareceres;
* execução;
* geração automática de jurisprudência;
* atualização autônoma de fontes sem revisão;
* plataforma web própria;
* servidor central obrigatório.

Esses recursos poderão ser objeto de versões futuras.

---

# 39. REGRAS DE IMPLEMENTAÇÃO

## DEV-001

Não implementar todo o sistema em um único commit.

## DEV-002

Cada alteração deverá representar unidade lógica coerente.

## DEV-003

Alterações arquiteturais relevantes deverão ser documentadas.

## DEV-004

Não utilizar APIs do Claude Code sem confirmar suporte na documentação oficial vigente.

## DEV-005

Não sobrescrever código funcional sem análise prévia.

---

# 40. PROCEDIMENTO INICIAL PARA O CLAUDE CODE

Ao receber esta SPEC, o agente deverá iniciar exclusivamente pela **Fase 0 — Auditoria**.

A auditoria deverá apresentar:

### A. Estado atual

* árvore do repositório;
* arquivos relevantes;
* Skills encontradas;
* templates;
* RAG;
* dependências;
* testes.

### B. Gap Analysis

Tabela com:

```text
ID
Status
Implementação encontrada
Lacuna
Ação recomendada
```

Status permitidos:

```text
[PRONTO]
[PARCIAL]
[AUSENTE]
[BLOQUEADO]
```

### C. Arquitetura proposta

Apresentar a árvore final recomendada.

### D. Riscos

Identificar:

* riscos técnicos;
* riscos jurídicos;
* riscos documentais;
* riscos de segurança;
* riscos de distribuição pública.

### E. Plano de implementação

Para cada fase:

```text
objetivo
arquivos afetados
riscos
testes
critério de aceite
```

### F. Gate

Ao final da Fase 0, o agente deverá parar.

Não iniciar implementação sem comando explícito:

```text
Pode executar a Fase 1.
```

---

# 41. DEFINIÇÃO DE PRONTO

Um requisito será considerado **DONE** apenas quando:

1. estiver implementado;
2. possuir teste aplicável;
3. o teste estiver passando;
4. estiver documentado quando necessário;
5. não violar nenhum invariante desta SPEC.

---

# 42. RESULTADO ESPERADO

A experiência final deverá ser aproximadamente:

```text
Advogado
   ↓
fornece documentos do processo
   ↓
solicita Contestação
   ↓
Contestação Skill
   ↓
extração factual
   ↓
tempestividade
   ↓
pesquisa jurídica
   ↓
RAG
   ↓
validação
   ↓
redator-peca-processual-elite
   ↓
humanizer-pt-br
   ↓
preenchimento dos placeholders
   ↓
Template Lock
   ↓
contestacao.docx
```

O advogado não deverá precisar manipular diretamente:

* Python;
* embeddings;
* banco vetorial;
* JSON;
* template;
* Git;
* infraestrutura interna.

---

# 43. PRINCÍPIOS CENTRAIS DA V1

A Arquitetura v1 deverá preservar quatro garantias fundamentais:

## 1. Integridade documental

O modelo institucional deverá permanecer protegido.

## 2. Confiabilidade jurídica

Toda fundamentação relevante deverá ser verificável.

## 3. Qualidade redacional

Toda Contestação deverá obrigatoriamente utilizar:

```text
redator-peca-processual-elite
+
humanizer-pt-br
```

## 4. Extensibilidade

Contestação será apenas o primeiro módulo de uma arquitetura destinada a múltiplos tipos de peças processuais.

---

# 44. REGRA FINAL

Esta SPEC é a fonte normativa inicial da Arquitetura v1.

Em caso de conflito entre implementação e esta especificação:

1. não corrigir silenciosamente;
2. registrar a divergência;
3. determinar se se trata de:

   * bug;
   * evolução legítima;
   * limitação técnica;
   * mudança arquitetural;
4. atualizar a SPEC ou criar ADR antes de consolidar decisão incompatível.

**A implementação deve servir à arquitetura definida, e não transformar decisões acidentais de código em arquitetura por inércia.**

---

# 45. INVARIANTES DE CALIBRAÇÃO REDACIONAL, PERTINÊNCIA FÁTICA E CONTROLE DE DECISÕES (ETAPA 5.2)

Registradas após o primeiro teste prático real da Contestação (Teste Real
01), que confirmou: parágrafos gerados pela IA excessivamente extensos;
redação prolixa; `SINOPSE_FATOS` contaminada por argumentação defensiva;
`RECONVENCAO` inserida sem autorização expressa do advogado;
`LICITUDE_CORTE_SUSPENSAO` inserido sem notícia concreta de corte; e
fundamentação normativa repetida na redação variável. Não revogam nem
enfraquecem nenhuma invariante anterior desta SPEC.

Princípio central: a IA complementa o texto-base institucional do modelo,
não compete com ele. A camada variável deve concentrar-se em fato
concreto + prova + aplicação ao caso + conclusão — não em compilação de
dispositivos legais.

## INV-PARAGRAFO-380

Todo parágrafo de conteúdo NOVO produzido pela IA para os placeholders
multiline da Contestação (`TEMPESTIVIDADE_CASO`, `SINOPSE_FATOS`,
`REALIDADE_FATICA`, `IRREGULARIDADE_ENCONTRADA`,
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`, `PEDIDOS_FINAIS` — os campos
`"multiline": true` de `templates/contestacao/schema.json`
`placeholder_contracts`) deve ter no máximo **380 caracteres com
espaços** (letras, números, espaços e pontuação). "Parágrafo" é cada
trecho separado por `\n` dentro do valor do placeholder — a mesma
convenção que `docx_template_engine.substituir_placeholders()` usa para
inserir `<w:br/>` entre linhas de um placeholder multiline.

Não se aplica ao conteúdo-base institucional fixo já existente no modelo
oficial (fora dos placeholders) — o validador (`scripts/
validate_paragrafos.py`) nunca lê o `document.xml`, só os valores dos
placeholders, o que por construção impede falso positivo no texto fixo.

Validado estruturalmente por `scripts/validate_paragrafos.py` — chamado
por `scripts/gerar_contestacao.py` como etapa própria do pipeline
(`stage=paragrafo_380`) antes do motor documental. Violação:
`PIPELINE_ABORTED` — nunca gera DOCX sabendo que a invariante foi
violada. Não corta texto mecanicamente: a decomposição do raciocínio em
mais parágrafos é trabalho do redator (`redator-peca-processual-elite`),
não do validador.

### TEST-046 — 380 caracteres

379 caracteres → aceito. 380 caracteres → aceito. 381 caracteres →
rejeitado. Conteúdo fixo institucional acima de 380 caracteres nunca
sofre falso positivo (o validador não o lê). Vários parágrafos, cada um
≤380, com um único parágrafo acima do limite → só o parágrafo violador é
reportado.

## INV-NAO-REDUNDANCIA

O limite de 380 caracteres não pode ser contornado por multiplicação
artificial de parágrafos que repetem a mesma conclusão com formulações
diferentes. Cada novo parágrafo deve acrescentar pelo menos um elemento
argumentativo útil: fato, prova, consequência, subsunção, distinção,
conclusão, ou fundamento efetivamente novo e necessário.

**Classificação: COMPORTAMENTAL, não determinística.** Não há
implementação Python para esta invariante — um detector semântico de
redundância argumentativa confiável não é tecnicamente viável sem
heurística jurídica embutida em código (vedada por CLAUDE.md §3/§6). A
disciplina é imposta pelas instruções de `redator-peca-processual-elite`
e pela revisão da Skill `contestacao` ao consumir a saída do redator, não
por um script de validação.

## INV-SINOPSE-ESTRITAMENTE-AUTORAL

`SINOPSE_FATOS` tem função exclusivamente narrativa: "o que a parte
autora afirma que aconteceu e o que pretende obter judicialmente?", com
fonte primária na petição inicial. Pode conter: relação jurídica
narrada, fatos alegados, evento que originou a demanda, condutas
atribuídas à ré, consequências alegadas, pedidos formulados, valores
pedidos quando relevantes, causa de pedir em síntese.

É proibido nela: versão da ré, análise comercial, argumentação
defensiva, impugnação, valoração da prova, contraposição entre inicial e
defesa, ou utilização do RAG para rebater o autor. Fatos oriundos
exclusivamente de análise comercial/relatório interno/documentação
técnica da ré pertencem a `REALIDADE_FATICA` e aos tópicos de defesa,
nunca à Sinopse. Se não for possível identificar a petição inicial com
segurança, a Sinopse não pode ser fabricada — fail-closed/interação,
mesmo padrão do restante do projeto.

**Classificação: HÍBRIDA.** `scripts/validate_placeholder_semantics.py`
implementa um backstop lexical determinístico (lista fechada de
marcadores defensivos: "não comprovou", "não merece prosperar", "ao
contrário do alegado", "os documentos demonstram", "não há prova", "a
cobrança é legítima", "regularidade da atuação", entre outros — rejeita
`PIPELINE_ABORTED`/`stage=placeholders` se encontrar qualquer um). Este
backstop é deliberadamente não exaustivo: ocorrência lexical prova
contaminação o bastante para reprovar, mas ausência de qualquer marcador
NÃO prova que a Sinopse está correta (texto defensivo bem escrito, sem
essas palavras específicas, não é pego). O controle substantivo é
comportamental (Skill `contestacao` + `redator-peca-processual-elite`).

### TEST-047 — Sinopse

Narrativa + pedidos da inicial → aceita. Presença de "não comprovou" →
rejeitada. Versão da análise comercial da ré → rejeitada. Conclusão sobre
legitimidade da cobrança → rejeitada. Sinopse objetiva, autoral, com
pedidos → aceita.

## INV-RECONVENCAO-AUTORIZACAO-EXPRESSA

Reconvenção é decisão processual reservada ao advogado — o estrategista
não pode decidi-la sozinho. `RECONVENCAO`
(`templates/contestacao/blocos.json`) tem `decision_mode: "humano"` (não
mais `"estrategista"`), mesmo contrato de decisão de
`decisoes_blocos.json` de antes ({`decisao`: `INCLUIR`/`EXCLUIR`/
`INDETERMINADO`}) — a diferença é de origem exigida: a Skill
`contestacao` deve perguntar `AskUserQuestion` ("Deseja apresentar
reconvenção neste processo?", SIM/NÃO) antes de registrar a decisão.

Sem resposta expressa, o bloco permanece ausente/`INDETERMINADO` em
`decisoes_blocos.json` — o fail-closed já existente para blocos manuais
(`stage=decisao_ausente`/`decisao_indeterminada`, `PIPELINE_ABORTED`)
cobre integralmente este caso, sem mecânica nova. Nunca presumir SIM a
partir de irregularidade, valor recuperável, tese favorável, histórico de
casos semelhantes, ou existência do bloco no template. Resposta NÃO →
`RECONVENCAO = EXCLUIR`; `INLINE_COM_RECONVENCAO` (linked) segue
automaticamente, sem resíduo textual "COM RECONVENÇÃO" no título.
Resposta SIM → `INCLUIR` só é aceito se as dependências existentes
(`VALOR_FRA` não-sentinela) estiverem satisfeitas.

### TEST-048 — Reconvenção

Possibilidade material + nenhuma resposta → `PIPELINE_ABORTED`.
Possibilidade material + NÃO → `RECONVENCAO`/`INLINE_COM_RECONVENCAO`
= `EXCLUIR`, sem "COM RECONVENÇÃO" no título final. Possibilidade
material + SIM + dependências completas → `INCLUIR`. SIM + `VALOR_FRA`
sentinela → fail-closed (`stage=placeholders`).

## INV-BLOCO-SUPORTE-FATICO

Nenhum tópico variável (bloco condicional do catálogo) pode ser incluído
simplesmente porque existe no catálogo, é juridicamente possível, é comum
nesse tipo de processo, o RAG recuperou legislação relacionada, ou o
estrategista o considera genericamente interessante. Fluxo: fato/
alegação/estado processual com proveniência → há suporte para
aplicabilidade do bloco? Não → `EXCLUIR`. Sim → bloco torna-se elegível →
análise estratégica → `INCLUIR`/`EXCLUIR`. O suporte pode decorrer,
conforme a tese, de petição inicial, decisão judicial, documento
processual, análise comercial, documento técnico, ou informação expressa
do advogado — não precisa ser fato incontroverso; a própria alegação do
autor pode tornar uma tese pertinente.

**Classificação: majoritariamente COMPORTAMENTAL** (princípio geral,
imposto pelas instruções de `estrategista-contestacao-ede` e da Skill
`contestacao`) **com duas instâncias DETERMINÍSTICAS** —
`INV-GRATUIDADE-LINKED` (vínculo puro) e `INV-CORTE-GATE-HUMANO` (gate
fático + decisão humana), abaixo — onde o suporte fático é redutível a
um booleano verificável sem heurística jurídica, ainda que a decisão de
incluir a tese (no caso do corte) permaneça humana.

## INV-GRATUIDADE-LINKED

`PRELIMINAR_REVOGACAO_GRATUIDADE` **não é decisão do estrategista de
espécie alguma** — nem livre, nem sob gate. É vinculada
DETERMINISTICAMENTE ao estado processual `GRATUIDADE_CONCEDIDA`: o
estado do bloco *é* o estado processual, ponto final. Não há etapa
intermediária de "elegibilidade" seguida de decisão estratégica (essa
era a modelagem original da Etapa 5.2, corrigida por auditoria antes do
commit — ver "Correção pontual" abaixo). `GRATUIDADE_CONCEDIDA: true` →
`INCLUIR` automático. `GRATUIDADE_CONCEDIDA` ausente, `false`, ou apenas
requerida sem decisão (mero pedido de gratuidade, declaração de
hipossuficiência sem decisão, pedido pendente) → `EXCLUIR` automático —
a ausência simples de informação NUNCA vira ambiguidade.
`GRATUIDADE_CONCEDIDA: "INDETERMINADO"` (documentos genuinamente
contraditórios/insuficientes) → aborta para interação com o advogado. O
estrategista pode desenvolver o conteúdo do bloco quando presente, mas
não decide sua existência — decisão manual em `decisoes_blocos.json`
para este bloco é rejeitada explicitamente, nunca ignorada em silêncio.

### Correção pontual (pré-commit): de gate a vínculo determinístico

A primeira implementação desta invariante modelou
`PRELIMINAR_REVOGACAO_GRATUIDADE` como `decision_mode: "estrategista"` +
`requires_fact: {"key": "GRATUIDADE_CONCEDIDA"}` — um GATE fático que só
restringia `INCLUIR`, deixando a decisão `INCLUIR`/`EXCLUIR` para a
etapa estratégica quando o fato fosse verdadeiro. Auditoria do usuário
identificou a divergência: o requisito aprovado ("a preliminar de
gratuidade deve ser linked") exige que o estado do bloco *acompanhe*
deterministicamente o estado processual — sem nova decisão do
estrategista entre as duas etapas. Corrigido antes do commit da Etapa
5.2 (nenhum commit havia sido feito) para `decision_mode: "state_linked"`
(ver arquitetura técnica abaixo). `LICITUDE_CORTE_SUSPENSAO` preservou o
mecanismo de gate original nesta etapa — depois migrou para o mesmo
`state_linked` numa correção arquitetural posterior, por achado real
análogo, e finalmente migrou de volta para gate (`requires_fact`) numa
3ª correção (Etapa 5.5), desta vez combinado com `decision_mode:
"humano"` em vez de `"estrategista"` puro (ver `INV-CORTE-GATE-HUMANO`,
abaixo): ao contrário da gratuidade, o requisito de negócio para corte
NÃO é "vínculo puro" — mesmo com o fato comprovado, a inclusão continua
sendo decisão do advogado.

### Arquitetura técnica (state_linked: estado processual → bloco)

O mecanismo `decision_mode: "linked"` pré-existente (usado por
`INLINE_COM_RECONVENCAO`) é estritamente bloco → bloco (`linked_to`
aponta para outro `id` do catálogo, cujo estado já resolvido é copiado).
Não é reaproveitável aqui sem falsificar um `linked_to` inexistente — o
vínculo necessário é estado processual → bloco, não bloco → bloco.
Extensão mínima adotada: um novo `decision_mode: "state_linked"`,
distinto de `"linked"`, com campo declarativo `linked_fact: "<ESTADO>"`
por bloco, e um novo parâmetro opcional `fatos_processuais` em
`docx_block_engine.validar_e_resolver_decisoes`/`gerar_peca_com_blocos`
— um dict `{ESTADO: bool | "INDETERMINADO" | {"valor": ..., ...}}`
carregado de `estado_processual.json` (novo arquivo opcional no
diretório do caso, mesmo padrão de `tempestividade.json`) por
`scripts/gerar_contestacao.py`. Blocos `state_linked` **nunca aceitam
decisão manual** em `decisoes_blocos.json` (mesmo tratamento fail-closed
de `derived`/`linked`: decisão fornecida é rejeitada, `stage=decisao_
invalida`, nunca ignorada). Resolução puramente mecânica
(`_estado_fato_processual`): chave ausente → `FALSE` → `EXCLUIR`; `true`
→ `INCLUIR`; `false` → `EXCLUIR`; `"INDETERMINADO"` → aborta
(`decisao_indeterminada`); qualquer outro valor → aborta
(`estado_processual_invalido`). Resolvido ANTES dos containers
`CONTAINER_DERIVED` (um bloco `state_linked` pode ser filho de um
container, como `PRELIMINAR_REVOGACAO_GRATUIDADE` é filho de
`PRELIMINARES`). Não altera nem reduz o comportamento de
`linked`/`INLINE_COM_RECONVENCAO`, pré-existente — mecanismo adicional,
não substituição. `requires_fact` (gate + decisão estratégica/humana)
também não foi removido do motor — permanece disponível como
infraestrutura geral, e desde a Etapa 5.5 volta a ser usado por
`LICITUDE_CORTE_SUSPENSAO` (`INV-CORTE-GATE-HUMANO`, abaixo).

### TEST-049 — Gratuidade

Gratuidade expressamente concedida (`GRATUIDADE_CONCEDIDA: true`, sem
decisão do estrategista) → `INCLUIR` automático. Indeferida/ausente →
`EXCLUIR` automático. Inicial apenas pede gratuidade (sem
`estado_processual.json` ou com `false`) → `EXCLUIR` automático.
Declaração de hipossuficiência sem decisão → `EXCLUIR` automático. Estado
genuinamente ambíguo (`"INDETERMINADO"`) → `PIPELINE_ABORTED`
(`decisao_indeterminada`). Decisão manual do estrategista para este
bloco (qualquer valor, mesmo coincidente com o fato real) →
`PIPELINE_ABORTED` (`decisao_invalida`) — rejeitada explicitamente, nunca
ignorada. Gratuidade concedida + outro filho de `PRELIMINARES` excluído
→ `PRELIMINARES = INCLUIR` (`ANY_CHILD_INCLUDED` preservado).

## INV-CORTE-GATE-HUMANO (Etapa 5.5, 3ª correção arquitetural)

`LICITUDE_CORTE_SUSPENSAO` é `decision_mode: "humano"` com
`requires_fact: {"key": "CORTE_EFETIVO"}` no catálogo — suporte fático
**e** decisão humana, nenhum dos dois dispensa o outro. Diferente de
`PRELIMINAR_REVOGACAO_GRATUIDADE`/`INV-GRATUIDADE-LINKED` (vínculo puro,
sem decisão alguma): aqui o corte efetivo comprovado só torna o bloco
**elegível** — a inclusão continua sendo decisão reservada ao advogado
(mesma filosofia de `INV-RECONVENCAO-AUTORIZACAO-EXPRESSA`, com um gate
fático adicional por cima).

Contrato de resolução (`docx_block_engine.validar_e_resolver_decisoes`):

- `CORTE_EFETIVO` ausente ou `false` → `EXCLUIR` automático, **sem
  pergunta ao advogado** (com ou sem decisão registrada em
  `decisoes_blocos.json` — se `EXCLUIR` estiver registrado, também é
  aceito; se `INCLUIR` estiver registrado, é rejeitado explicitamente,
  `gate_fatico_nao_satisfeito`). Nada a decidir sobre uma tese sem
  suporte fático; não desperdiça interação humana com uma pergunta cuja
  resposta os fatos já determinam.
- `CORTE_EFETIVO: "INDETERMINADO"` → aborta (`decisao_indeterminada`),
  com ou sem decisão registrada — nunca presumir `true` nem `false`
  quando houver contradição factual relevante.
- `CORTE_EFETIVO: true` **sem** decisão registrada → aborta
  (`decisao_ausente`) — sinal para a Skill `contestacao` perguntar
  (`AskUserQuestion`) "Deseja incluir o tópico relativo à licitude do
  corte/suspensão?" antes de prosseguir; nunca gera o DOCX final calado
  nesse estado.
- `CORTE_EFETIVO: true` **com** decisão humana registrada → `INCLUIR`/
  `EXCLUIR` conforme a resposta dada.

### Histórico da modelagem: 3 correções, cada uma por achado real

(1) **Modelagem original** (`decision_mode: "estrategista"` +
`requires_fact: {"key": "CORTE_EFETIVO"}`, sem `decision_mode: "humano"`)
permitia à etapa estratégica registrar `INCLUIR` sempre que o gate
estivesse satisfeito, sem checagem adicional — e, num teste real, o
bloco foi incluído com base numa alegação da própria autora na petição
inicial, tratada incorretamente como suporte fático suficiente.

(2) **Correção para `state_linked`** (mesmo mecanismo de
`INV-GRATUIDADE-LINKED`: estado processual → bloco, sem decisão alguma
do estrategista) — resolveu o achado (1), mas um teste real seguinte
mostrou que isso incluía o bloco automaticamente sempre que o corte
fosse comprovado, mesmo quando o advogado não manifestou vontade de
desenvolver a tese. Requisito de negócio para corte não é "vínculo
puro" como o de gratuidade.

(3) **Correção atual — `requires_fact` + `decision_mode: "humano"`**
(Etapa 5.5): combina os dois requisitos das correções anteriores —
suporte fático (do achado 1) e controle humano explícito (do achado 2)
— sem repetir nenhum dos dois erros. Reaproveitamento do mecanismo
genérico já existente no motor (nenhuma arquitetura paralela criada):
`requires_fact` nunca foi removido do motor, só ficou temporariamente
sem uso entre as correções (1)→(2).

**Alegação da autora não é prova de corte efetivo — achado real que
motivou a 1ª correção, ainda válido**: a petição inicial pode pedir
restabelecimento, mencionar suspensão, ameaça, risco, negativação ou
inadimplência sem que isso demonstre que a suspensão de fato ocorreu —
pedido/alegação processual da parte autora **não é prova automática do
fato**. Distinguir sempre: ALEGAÇÃO/PEDIDO DA AUTORA ≠ FATO COMPROVADO ≠
ESTADO PROCESSUAL. `CORTE_EFETIVO: true` exige evidência que a própria
Ré não contradiga (ex.: registro operacional da concessionária,
documento do caso que confirme a suspensão) ou resposta expressa do
advogado (`AskUserQuestion`, mesmo padrão de
`INV-TEMPESTIVIDADE-MARCO`/`INV-RECONVENCAO-AUTORIZACAO-EXPRESSA`)
quando os documentos disponíveis não bastarem para confirmar de forma
independente da narrativa da própria autora. Isto é uma **exceção
expressa** ao princípio geral de `INV-BLOCO-SUPORTE-FATICO` (que aceita
alegação da autora como suporte suficiente para a maioria dos blocos,
ver seção correspondente) — `CORTE_EFETIVO`, por condicionar uma tese
defensiva específica com histórico de inclusão indevida, exige padrão
mais rigoroso: alegação isolada e não corroborada da autora nunca basta
sozinha, mesmo quando `INV-BLOCO-SUPORTE-FATICO` bastaria para outro
bloco.

Proibido inferir `CORTE_EFETIVO: true` isoladamente a partir de menções
a: "restabelecimento", "suspensão", "ameaça", "risco", "negativação",
"inadimplência", "débito", "religação", "abstenção de corte" — sozinhas,
sem confirmação independente da narrativa da autora. Vedação de busca
lexical também permanece: a frase "não houve corte" num documento não
vira `CORTE_EFETIVO: true`; o estado é resolvido pela Skill `contestacao`
a partir da leitura e compreensão dos documentos (proveniência, mesmo
contrato de REQ-030), nunca por ocorrência textual verificada em código.

### TEST-050 — Corte/suspensão (revisado, Etapa 5.5)

`CORTE_EFETIVO: false` → `EXCLUIR` automático, sem pergunta. Ausente →
`EXCLUIR` automático, sem pergunta (mesmo efeito de falso — fail closed
por omissão, nunca por presunção de verdadeiro). `CORTE_EFETIVO:
"INDETERMINADO"` → `PIPELINE_ABORTED` (`decisao_indeterminada`),
interação com o advogado necessária. `CORTE_EFETIVO: true` **sem**
decisão humana registrada → `PIPELINE_ABORTED` (`decisao_ausente`) — o
pipeline para e pede a decisão. `CORTE_EFETIVO: true` **com** decisão
humana `INCLUIR` → bloco incluído. `CORTE_EFETIVO: true` **com** decisão
humana `EXCLUIR` → bloco excluído. Mera ameaça + `CORTE_EFETIVO: false`
→ `EXCLUIR`, sem pergunta. Pedido de restabelecimento sem corte
comprovado → `EXCLUIR`, sem pergunta. Inadimplência/débito +
`CORTE_EFETIVO: false` → `EXCLUIR`, sem pergunta. Tentar `INCLUIR` com
`CORTE_EFETIVO: false` → `PIPELINE_ABORTED` (`gate_fatico_nao_
satisfeito`) — rejeitado mesmo com decisão humana registrada. Texto
contendo "não houve corte" não é interpretado como `CORTE_EFETIVO:
true`. Bloco `EXCLUIR`: nenhum texto residual sobre licitude do corte no
DOCX final; `PEDIDOS_FINAIS` não reintroduz a tese
(`PALAVRAS_CHAVE_BLOCO_PEDIDOS` inclui `LICITUDE_CORTE_SUSPENSAO`).

## INV-NAO-REDUNDANCIA-NORMATIVA

A redação variável não deve repetir dispositivo normativo (artigo,
inciso, parágrafo, resolução, dispositivo regulamentar) já presente no
conteúdo-base institucional fixo do tópico correspondente. Antes de
acrescentar qualquer dispositivo à redação variável: verificar o
conteúdo-base do tópico → se o fundamento necessário já está lá, não
repetir → se não estiver, consultar o RAG → só acrescentar dispositivo
novo quando houver necessidade jurídica concreta não coberta pelo
texto-base.

Princípio: o RAG não é gerador de lista de artigos — é
validação + complementação necessária. Continua obrigatório onde já era
obrigatório (§7, REQ-017 a REQ-025); pode validar silenciosamente a
fundamentação normativa institucional já presente no tópico, o que não
significa reproduzi-la de novo no texto variável.

**Classificação: COMPORTAMENTAL** — imposta pelas instruções de
`redator-peca-processual-elite` e `estrategista-contestacao-ede`; não há
detector Python confiável de "dispositivo já coberto pelo texto-base"
sem risco de falso positivo/negativo em linguagem jurídica natural.

---

Todas as invariantes desta seção estão refletidas, em versão resumida,
em `CLAUDE.md` §7 ("Invariantes de calibração redacional e composição —
Etapa 5.2"). As Skills (`skills/contestacao/SKILL.md`,
`skills/estrategista-contestacao-ede/SKILL.md`,
`skills/redator-peca-processual-elite/SKILL.md`) explicam COMO cumpri-las
operacionalmente — não duplicadas literalmente aqui.

---

# 46. INV-CONTESTACAO-SEM-PESQUISA-JURISPRUDENCIAL (achado do Teste Real 01-B)

## Origem do achado

Durante o Teste Real 01-B, foi observado o comportamento "Calling
Jurisprudências.ai 5 times" — uma ferramenta/MCP externo de
jurisprudência sendo acionado durante a elaboração da Contestação. Este
comportamento nunca foi autorizado pela arquitetura do EDE Legal Plugin.

Auditoria forense identificou a causa habilitante: `skills/contestacao/
SKILL.md` §7 (antes da primeira correção) afirmava que "uma citação
jurisprudencial só pode vir de confirmação manual do advogado **ou de
pesquisa externa explícita**" — a única ocorrência, em todo o
repositório, de uma instrução que nomeia "pesquisa externa" como fonte
aceitável de jurisprudência. Combinada com uma ferramenta externa de
jurisprudência eventualmente conectada à sessão (integração alheia ao
plugin — `.claude-plugin/plugin.json` não declara nenhum MCP, e o
`CHANGELOG.md` registra "0 MCP servers" desde a distribuição), essa
instrução constituía um caminho plausível para o agente invocar a
ferramenta disponível ao interpretar "pesquisa externa explícita"
literalmente. Não há, no ambiente desta auditoria, acesso ao registro
completo da sessão do Teste Real 01-B — a causa é apontada pela evidência
textual encontrada (única ocorrência do termo em todo o repositório), não
por reconstituição do raciocínio exato do agente que executou o teste.

A primeira correção (`INV-JURISPRUDENCIA-EXTERNA-DESABILITADA`) tratou o
achado como vedação de ferramenta *externa*, condicionada à resolução de
`PEND-002`. Auditoria de acompanhamento definiu arquitetura mais estrita:
a Contestação nunca trabalha com jurisprudência, ponto — decisão
permanente da peça, não uma vedação temporária ligada ao status de
`PEND-002`. Esta seção substitui/absorve a anterior; não há duas regras
redundantes.

## Regra

A Contestação é autossuficiente e estrita ao modelo institucional: sua
elaboração não realiza pesquisa jurisprudencial de espécie alguma —
decisão arquitetural da peça, independente do status de `PEND-002` (ver
"Auditoria de PEND-002" abaixo). A execução de `/contestacao` **nunca**:

* pesquisa jurisprudência (RAG, web, ou qualquer outro meio);
* aciona Jurisprudências.ai ou qualquer nome equivalente;
* aciona MCP de jurisprudência (o plugin não declara nenhum MCP próprio);
* aciona agente ou subagente de jurisprudência/precedentes;
* usa busca web para jurisprudência;
* usa API jurisprudencial de qualquer espécie;
* consulta tribunal;
* pesquisa precedentes por nenhum caminho, externo ou não;
* solicita automaticamente ao advogado que pesquise jurisprudência;
* interrompe ou bloqueia a geração da peça por ausência de
  jurisprudência.

Isso vale inclusive quando tal ferramenta está disponível no ambiente/
sessão do Claude Code por integração alheia ao plugin — disponibilidade
não é autorização de uso.

## Estrita ao modelo

A Contestação parte do conteúdo jurídico institucional já existente no
modelo oficial: compreende os documentos, identifica os fatos, seleciona
os blocos pertinentes (`INV-COMPOSICAO-BLOCOS`), aplica o conteúdo
institucional ao caso concreto, complementa apenas o necessário
(legislação/regulamentação local validada — nunca jurisprudência), redige
a camada variável e preserva a estrutura jurídica do modelo. Não
transforma cada Contestação em pesquisa jurídica aberta. Súmula/
precedente/entendimento jurisprudencial só aparece onde já preservado no
conteúdo-base fixo do modelo — nunca pesquisado para atualização,
substituído, ou expandido durante a elaboração de um caso.

O redator não produz frases como "conforme entendimento consolidado do
STJ...", "segundo jurisprudência pacífica...", "os tribunais têm
entendido...", "é pacífico na jurisprudência..." — salvo quando a
afirmação já integrar legitimamente o conteúdo-base institucional
preservado. A persuasão decorre de fatos + provas + modelo institucional
+ legislação/regulamentação local validada + subsunção — jurisprudência
não é um desses ingredientes nesta peça.

O estrategista trabalha com o arsenal jurídico autorizado pelo modelo
(REN ANEEL 1.000/2021, CPC, CDC, fatos do caso). Ausência de precedente
não constitui lacuna a preencher — não produz mais o marcador
`"PESQUISA JURISPRUDENCIAL NECESSÁRIA."` (obsoleto para esta peça). Se
uma tese só puder ser sustentada mediante jurisprudência, isso é sinal de
deficiência estrutural do próprio modelo institucional — registrado como
achado para revisão humana futura do modelo, nunca resolvido por pesquisa
autônoma.

## Auditoria de PEND-002

`PEND-002` ("Indexação de jurisprudência (REQ-018) na busca híbrida",
`docs/PENDENCIAS.md`) tem finalidade distinta desta invariante: é uma
decisão sobre a INFRAESTRUTURA do RAG (se/como indexar o corpus sensível
de `rag/jurisprudencia/` — 57 fichas com dados reais de clientes — na
busca híbrida geral do projeto), não especificamente "adicionar
jurisprudência à Contestação". Por isso `PEND-002` **não foi encerrada**
por esta correção — permanece `ABERTA`/adiada, com seu próprio critério
de resolução em `docs/PENDENCIAS.md`. O que muda: mesmo que `PEND-002`
seja resolvida no futuro (jurisprudência indexada no RAG), a Contestação
especificamente continuará sem consumi-la para citação, por força desta
invariante — as duas decisões são independentes uma da outra. Uma nota
cruzando as duas foi adicionada a `docs/PENDENCIAS.md`.

## Não afeta

O RAG jurídico local para legislação/regulamentação
(`rag/search_hybrid.py`, corpus de CPC/CC/CDC/L8987/L9427/REN1000,
embeddings, `rag/legal_validation/`) continua integralmente autorizado e
obrigatório onde já era — REQ-017 a REQ-025 inalterados. Esta invariante
restringe exclusivamente o uso de jurisprudência (pesquisada ou não) pela
Contestação.

## TEST-051 — Governança contra pesquisa jurisprudencial (regra permanente)

As três Skills da Contestação (`contestacao`, `estrategista-contestacao-
ede`, `redator-peca-processual-elite`) não podem conter instrução
positiva para: pesquisar jurisprudência (por qualquer meio); buscar
precedentes; chamar MCP de jurisprudência; chamar Jurisprudências.ai (ou
nome equivalente) como fonte válida; consultar ferramenta externa de
jurisprudência; ou solicitar pesquisa jurisprudencial como etapa normal
do fluxo. O teste sobrevive à existência futura de qualquer ferramenta de
pesquisa (Jurisprudências.ai, MCP jurídico, busca web) no ambiente —
disponibilidade da ferramenta não é autorização de uso, e o teste audita
o TEXTO das Skills, não o ambiente de execução.

---

# 47. CALIBRAÇÃO FINAL DO TESTE REAL 01-B (ETAPA 5.3)

Sete achados reais do Teste Real 01-B, cada um com diagnóstico, correção
e teste. Critério geral: "MODELO = BASE JURÍDICA. IA = ADAPTAÇÃO AO CASO
CONCRETO" — a Contestação deve se aproximar do comportamento de um
advogado que utiliza um modelo institucional consolidado, não reescrever
a peça inteira a cada caso.

## Endereçamento institucional

Achado: endereçamento frágil/genérico ("Vara Cível da Comarca de...").
Correção: `JUIZO` sempre em CAIXA ALTA, padrão `AO JUÍZO DA VARA [...]
DA COMARCA DE [...]` — validado por `scripts/
validate_placeholder_semantics.py` (`_validar_juizo`). Não valida a
parte central (varia por especialização de vara/comarca — decisão da
Skill/advogado, nunca regex). Integração futura com API/DJEN para nome
exato do órgão julgador fica **fora de escopo** desta etapa — não
implementada, sem chamada de API/MCP/busca web.

## Fonte do `PROCESSO Nº`

Achado: renderizado em 14pt (`w:sz w:val="28"`) no template real.
Correção: template oficial local corrigido para 12pt (`w:sz
w:val="24"`) nos 4 runs correspondentes ("PROCESSO Nº " × 2 + "
{{NUMERO_PROCESSO}}" × 2, par `mc:Choice`/`mc:Fallback`) — edição
cirúrgica verificada por diff caractere-a-caractere contra um round-trip
de controle sem edição (confirmando que SÓ esses 4 caracteres mudaram em
todo o `document.xml`, 403570 caracteres). Testado por
`test_processo_no_fonte_12_no_template_real`
(`tests/test_template_engine.py`, LOCAL_ONLY).

## Normalização do `AUTOR`

Achado: nome não estava em caixa alta. Correção: `AUTOR` sempre em CAIXA
ALTA — validado por `_validar_autor` (mesma função que já rejeitava CPF/
cláusula de qualificação, Etapa 3).

## Proibição de meta-informação (achado grave)

Achado: `"A citação da Ré nestes autos foi realizada em 29/07/2026,
conforme informação processual fornecida pelo advogado subscritor desta
peça."` — o documento revelou sua própria mecânica interna. Correção:
backstop lexical (`_checar_meta_proveniencia`, lista ampla de variações
de fraseado) aplicado a todos os placeholders narrativos/técnicos
(`TEMPESTIVIDADE_CASO`, `SINOPSE_FATOS`, `REALIDADE_FATICA`,
`IRREGULARIDADE_ENCONTRADA`, `DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`,
`PEDIDOS_FINAIS` — à época incluía também
`ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA`, removido definitivamente em
correção pontual posterior, ver §49). Proveniência
(`marco_origem`, `source_document`) permanece só nos arquivos internos
(`fatos.json`, `tempestividade.json`) — nunca verbalizada na peça.

## Diagnóstico estatístico dos parágrafos e regra de densidade

Medição empírica sobre a fixture sintética `tests/fixtures/contestacao/
happy_path/placeholders.json` (proxy razoável do padrão de saída do
redator, não uma amostra real do Teste Real 01-B — não disponível neste
ambiente): nenhum parágrafo isolado excedia 301 caracteres, mas
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE` somava 874 caracteres em 6
parágrafos e `PEDIDOS_FINAIS` somava 715 em 5 — confirma a hipótese do
achado (acumulação, não tamanho unitário). `scripts/
validate_paragrafos.py` (`LIMITES_DENSIDADE_BLOCO`,
`validar_densidade_blocos`) impõe teto de parágrafos E de caracteres
totais por bloco: `SINOPSE_FATOS`/`REALIDADE_FATICA` (3/600),
`IRREGULARIDADE_ENCONTRADA` (2/600),
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE` (5/700), `PEDIDOS_FINAIS`
(2/400) — à época também `ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA` (3/500),
removido definitivamente em correção pontual posterior, ver §49.
Primeira hipótese documentada, não definitiva — a recalibrar
com dados de um teste real. Novo `stage=densidade_bloco` no pipeline
(`scripts/gerar_contestacao.py`), `PIPELINE_ABORTED` se violado.

## Sinopse — tempo verbal e compactação

Estrutura semântica preservada (já considerada boa) — só ajuste de
forma: tempo verbal uniforme (presente do indicativo preferencial, ou
pretérito perfeito para fato concluído) e compactação real (resumo, não
reprodução quase cronológica da inicial). Comportamental — sem validação
Python além do teto de densidade acima (que já força compactação
indiretamente).

## Irregularidade — destaque do tipo + assinatura do TOI

Nome do tipo em `**negrito**`/caixa alta, integrado à frase, seguido de
parágrafo técnico explicativo (objetivo, específico, sem inventar
característica não comprovada — distinção entre TIPO identificado e
DESCRIÇÃO FÁTICA documentada). Suporte de engine novo em
`scripts/docx_template_engine.py`: convenção markdown `**texto**` no
valor do placeholder vira `<w:b/><w:bCs/>` real (posição correta na
sequência CT_RPr, cor FF0000 preservada) — implementado como extensão do
substituidor de placeholders existente (`_corpo_com_negrito`,
`_segmentar_negrito`, `_rpr_vermelho_negrito`), caminho antigo
(sem `**`) byte-idêntico ao anterior à Etapa 5.3. Testado com XML
sintético e contra o template real (LOCAL_ONLY,
`tests/test_template_engine.py`). Assinatura do TOI nunca mencionada na
peça (`_checar_assinatura_toi`, backstop lexical em
`IRREGULARIDADE_ENCONTRADA`/`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`) —
pode existir na proveniência interna, nunca na redação defensiva.

## Modelo × subsunção, papel do RAG, não-redundância normativa

Reforço comportamental de `INV-NAO-REDUNDANCIA-NORMATIVA` (Etapa 5.2): a
redação variável não reexplica artigo por artigo o que o tópico-base já
traz (achado real: sequência "art. 589... art. 590... art. 591..." em
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`). RAG local
(`rag/search_hybrid.py`) permanece obrigatório onde já era — mas
RECUPERAÇÃO ≠ INSERÇÃO, VALIDAÇÃO ≠ REDAÇÃO; não decidido por
heurística Python destrutiva (risco de apagar artigo legitimamente
necessário) — disciplina de Skill + testes, não validador agressivo.

## Pedidos — redesenho e vínculo composicional determinístico

Achado: `PEDIDOS_FINAIS` respondendo individualmente a quase todos os
pedidos da inicial — "uma segunda contestação". Correção comportamental:
seção curta, institucional, conclusiva (núcleo "julgar improcedentes os
pedidos da inicial"), sem cascata de subsidiários sem suporte real.
Correção determinística nova: `scripts/docx_block_engine.py`
(`PALAVRAS_CHAVE_BLOCO_PEDIDOS`, `validar_pedidos_composicionais`) —
`PEDIDOS_FINAIS` não pode mencionar tese de bloco `EXCLUIR` (gratuidade,
inaplicabilidade do CDC, reconvenção — backstop lexical, mesma limitação
de qualquer backstop: presença prova incoerência, ausência não prova
coerência). Novo `stage=pedidos_composicionais` no pipeline, antes da
composição do DOCX.

## Local e data

`LOCAL_DATA` sempre "Salvador", independentemente da comarca do processo
— validado por `_validar_local_data` (extensão da checagem já existente
de tamanho/narrativa).

## Overflow de imagem/rodapé — limitação documentada, não corrigido

Achado visual confirmado no Teste Real 01-B: elemento gráfico
ultrapassando a área útil, colidindo com o rodapé. Diagnóstico OOXML
realizado: os 19 `<wp:anchor>` do template real usam todos `positionV
relativeFrom="paragraph"` — posicionamento relativo ao parágrafo-âncora,
inerentemente sensível a reflow (se conteúdo variável antes da âncora
crescer, a âncora desce, podendo empurrar o elemento para a área do
rodapé). Isso corresponde à hipótese (C) do checklist do achado
("conteúdo anterior empurrou a âncora"). **Não corrigido nesta etapa**:
este ambiente não tem renderizador (LibreOffice/`soffice` ausente) para
confirmar visualmente qual anchor específico colide e validar que uma
correção XML não desloca outros elementos do timbrado — aplicar uma
mudança de anchor "às cegas" seria mais arriscado que não corrigir
(CLAUDE.md §17, §25 do prompt da Etapa 5.3: "menor correção possível",
nunca redesenho sem causa confirmada). Recomendação: no próximo teste
real, capturar página/parágrafo exato da colisão para permitir correção
cirúrgica (`keepNext`/`pageBreakBefore` no parágrafo-âncora específico,
ou `relativeFrom="page"` só naquele anchor) com confiança visual.

## FF0000 preservado

Nenhuma alteração à regra de cor — todo conteúdo gerado continua FF0000
(inclusive os novos runs em negrito do §9); texto fixo institucional
nunca muda de cor. Testes de regressão de Etapa 3 continuam verdes.

# 48. NORMALIZAÇÃO VISUAL DE PARÁGRAFOS E CONTRATO ATÔMICO DA IRREGULARIDADE (ETAPA 5.3-B)

## Diagnóstico confirmado (sem heurística)

Comparação real (Teste Real 01-C) entre parágrafo nativo do modelo e
parágrafo produzido pelo motor: `w:pPr`/`w:rPr` estavam corretamente
herdados no parágrafo-placeholder original; fonte, tamanho, FF0000 e
alinhamento-base não eram a causa. A causa raiz: valores multiline de
placeholder (`SINOPSE_FATOS`, `REALIDADE_FATICA`,
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`, `PEDIDOS_FINAIS` etc.) eram
representados como múltiplas `<w:t>` separadas por `<w:br/>` **dentro de
um único `<w:p>`**. O OOXML aplica `w:spacing`/`w:before`/`w:after`/
`contextualSpacing`/demais propriedades de parágrafo à unidade `<w:p>`
— três parágrafos lógicos assim comprimidos recebiam o espaçamento do
modelo só uma vez (ao redor do bloco inteiro), nunca entre si, o que os
fazia destoar visualmente de parágrafos nativos do modelo (cada um sua
própria `<w:p>`, cada um recebendo `w:spacing` individualmente).

## INV-PARAGRAFO-HERDA-TEMPLATE

Todo `<w:p>` novo criado a partir de conteúdo multiline de um placeholder
deve herdar estruturalmente (clonagem/deep copy do `w:pPr` original, não
reconstrução manual propriedade a propriedade) as propriedades do
parágrafo-placeholder que lhe deu origem. TEMPLATE define aparência
(fonte de verdade visual); PIPELINE só fornece conteúdo.

**Implementação** (`scripts/docx_template_engine.py`):

- `_corpo_com_negrito` passou a marcar (namespace próprio, autocontido no
  elemento — `urn:ede:motor:marcador-quebra-multilinha`) tanto cada
  `<w:br/>` de fronteira entre linhas lógicas quanto cada `<w:t>` de
  conteúdo por ela gerado. Deliberadamente **não** usa a cor FF0000 como
  sinal — inferir "vermelho + `<w:br/>`" seria frágil (§8 do prompt da
  Etapa 5.3-B): um `<w:br/>` vermelho poderia existir por outro motivo no
  futuro sem ser fronteira de parágrafo lógico.
- `_explodir_paragrafos_multilinha` (novo, chamado ao final de
  `substituir_placeholders`) percorre o XML já substituído via `lxml` e,
  para cada `<w:br/>` marcado, corta o `<w:p>` corrente ali: cria um
  `<w:p>` irmão clonando o `w:pPr` do parágrafo corrente, move para ele
  os elementos subsequentes que sejam comprovadamente nossos (marcador ou
  `<w:t>` marcado) e **para** de mover no primeiro elemento não marcado —
  necessário porque `IRREGULARIDADE_ENCONTRADA` tem texto FIXO do
  template (sufixo ", circunstância que...") na própria `<w:p>` logo após
  o placeholder; sem esse corte, esse texto fixo seria arrastado para o
  parágrafo novo errado. Ao final, os atributos de marcação são removidos
  de todo `<w:t>` sobrevivente e `lxml.etree.cleanup_namespaces` remove a
  declaração `xmlns:ede` residual — o marcador nunca alcança o schema
  OOXML/`validate.py`.
- Quebras `<w:br/>` legítimas pré-existentes no template (sem o
  atributo do namespace próprio) nunca são tocadas.
- Idempotente por construção: a função só consome marcadores, nunca cria
  novos — reexecutá-la sobre a própria saída é no-op.
- Placeholders de uma linha só (a maioria) seguem o caminho rápido
  anterior, byte-idêntico ao comportamento pré-Etapa-5.3.

## Contrato atômico de `IRREGULARIDADE_ENCONTRADA` — reversão de §9 (Etapa 5.3)

Causa raiz da duplicação observada no Teste 5.3 (`"Na ocasião, foi
constatada irregularidade do tipo, Na ocasião, foi constatada
irregularidade do tipo DESVIO DE ENERGIA..."`): confirmada via inspeção
XML como **defeito de conteúdo, não do Template Engine**. O template
oficial já contém, como texto fixo em runs separados ao redor do
placeholder: `"Na ocasião, foi constatada irregularidade do tipo, "` +
`{{IRREGULARIDADE_ENCONTRADA}}` + `", circunstância que impedia o
registro integral da energia efetivamente consumida na unidade."` — o
valor fornecido para o placeholder repetia o prefixo, o tipo E o sufixo
fixos.

Novo contrato (§14-§18 do prompt da Etapa 5.3-B): `IRREGULARIDADE_
ENCONTRADA` contém EXCLUSIVAMENTE o nome/tipo da irregularidade, em
`**negrito**`, minúsculas/redação natural (ressalvados nomes próprios/
siglas — **reverte** a orientação "negrito + caixa alta" da Etapa 5.3
§9, mantida apenas para outros campos que já a exigiam, ex.: `AUTOR`/
`JUIZO`). A explicação técnica do tipo não entra neste campo — vai em
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`, que o template já renderiza em
parágrafo(s) subsequente(s) próprio(s).

Backstop lexical específico (não blacklist genérica — não pode rejeitar
tipos de irregularidade válidos) em `scripts/
validate_placeholder_semantics.py` (`_validar_irregularidade_encontrada`):
rejeita fragmentos do texto fixo do template ("na ocasião", "foi
constatada", "irregularidade do tipo", "circunstância que", "impedia o
registro") e rejeita o campo integralmente em caixa alta.

## Escopo não tocado

Confirmado intocado nesta etapa: RAG (`rag/`), estratégia jurídica,
decisões de blocos composicionais, imagens/anchors/`wp:anchor`,
cabeçalho/rodapé, `INV-PARAGRAFO-380`/`LIMITES_DENSIDADE_BLOCO`
(preservados como estavam), template oficial (`modelo-oficial.docx` não
alterado — hash de Etapa 5.3 preservado). O achado de overflow
imagem/rodapé (§47) permanece limitação documentada, não corrigido nesta
etapa (investigação separada, não misturada com normalização de
parágrafos, por instrução expressa).

# 49. REMOÇÃO DEFINITIVA DE ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA

## Decisão

O placeholder `ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA` foi removido
definitivamente de toda a arquitetura da Contestação — não renomeado,
sem alias, sem substituto equivalente. Motivo: o modelo oficial já
formula a argumentação institucional de evolução de consumo por
completo, como texto fixo dentro do bloco `EVOLUCAO_CONSUMO`; a IA
gerar novamente essa argumentação era redundância (mesma lógica de
INV-NAO-REDUNDANCIA-NORMATIVA, §7, aplicada aqui a um bloco inteiro em
vez de um dispositivo legal isolado).

## Contexto — auditoria que motivou a remoção

Ao auditar o template oficial nesta correção, constatou-se que o asset
externo atual (fornecido separadamente do plugin, ADR-0009) já não
contém `{{ARGUMENTACAO_EVOLUCAO_DE_CONSUMO_FIXA}}` em `word/
document.xml` — o bloco `BLOCO:EVOLUCAO_CONSUMO` (SDT preservado, `w:id
5000011`) passou a conter só texto institucional fixo ("Outrossim, a
concessionária procedeu ao levantamento de carga instalada...", "Ao
contrário do que levianamente alega a parte Autora, houve evidente
evolução de consumo...", "A média de consumo do empreendimento...").
O hash do template mudou desde o registrado ao final da Etapa 5.3-B
(`22c8631e...` → `399f34184864724663d9b9443d708581efc2ca562ba756352615c9def364abce`)
— alteração feita pelo advogado/escritório no asset externo, fora deste
plugin (comportamento esperado, ADR-0009: o template é mantido pelo
usuário autorizado). **Esta correção não editou o template** — a
remoção do marcador já estava feita quando a auditoria começou; o
trabalho aqui foi só de código/documentação, acompanhando essa mudança.

## PEND-006 — resolvida

`PEND-006` (fronteira do bloco `EVOLUCAO_CONSUMO` definida por
julgamento humano porque o placeholder aparecia duas vezes na região
candidata) está **resolvida por eliminação da causa**: sem placeholder
algum dentro do bloco, a ambiguidade de fronteira que motivou o
pendente não pode mais ocorrer. Ver `docs/PENDENCIAS.md`.

## O que mudou

- `templates/contestacao/schema.json`: removido de `editable_placeholders`
  e de `placeholder_contracts` (12 placeholders, era 13).
- `templates/contestacao/blocos.json`: `EVOLUCAO_CONSUMO` deixou de ser
  `CONDICIONAL_HIBRIDO` — agora `CONDICIONAL_PADRAO` (mesma categoria dos
  demais blocos puramente fixos), `placeholders: []`, `dependencies: []`.
  Decisão INCLUIR/EXCLUIR continua da etapa estratégica — só não recebe
  mais preenchimento algum.
- `scripts/gerar_contestacao.py`: removido de
  `PLACEHOLDERS_OBRIGATORIOS_NAO_VAZIOS`.
- `scripts/validate_paragrafos.py`: removido de `LIMITES_DENSIDADE_BLOCO`.
- `scripts/validate_placeholder_semantics.py`: removido validador
  dedicado (`_validar_evolucao_consumo`) e entrada em
  `VALIDADORES_ESPECIFICOS`.
- Fixtures/testes atualizados (ver relatório da correção para a lista
  completa de arquivos).

## Comportamento resultante

Quando `EVOLUCAO_CONSUMO` = `INCLUIR`: só o texto fixo institucional
aparece — uma única vez, sem paráfrase da IA. Quando `EXCLUIR`: o bloco
inteiro (SDT) é removido, texto fixo incluso. Este bloco não tem mais
preenchimento algum: se houver dado concreto do caso sobre evolução de
consumo, a complementação casuística só cabe em placeholder já existente
especificamente destinado a subsunção (nunca um novo placeholder
dedicado a este tópico); se não houver placeholder assim disponível ou
dado concreto suficiente, nenhuma complementação é criada — o texto fixo
do modelo é suficiente por si só (CLAUDE.md, seção correspondente).

# 50. INV-CONTESTACAO-SEM-TRAVESSAO

## Achado

Teste real identificou que `redator-peca-processual-elite` e
`humanizer-pt-br` estavam inserindo o travessão ("—", U+2014) no texto
gerado da Contestação. Auditoria das instruções ativas das duas Skills:
nenhuma instrui POSITIVAMENTE o uso de travessão — `humanizer-pt-br`
§13 ("Uso excessivo de travessão") já orientava reduzir a frequência do
caractere, com exemplo antes/depois correto. A causa mais provável é
estilística: as próprias instruções das Skills usam travessão com
frequência em sua prosa (redator especialmente), o que tende a induzir
o mesmo padrão no texto produzido, mesmo sem instrução explícita. Uma
segunda fonte, determinística e não relacionada às Skills, foi
encontrada nesta correção: `scripts/gerar_contestacao.py` construía o
valor de fallback de `TEMPESTIVIDADE_CASO` com um travessão literal no
código Python (corrigido, ver abaixo).

## Regra

Nenhum conteúdo textual gerado, reescrito, humanizado ou complementado
pela IA para os placeholders da Contestação pode conter o travessão. A
regra é independente do comportamento probabilístico das Skills — não
basta corrigir a instrução, há um backstop determinístico.

## Skills alteradas

- `skills/redator-peca-processual-elite/SKILL.md`: nova regra em
  "Regras próprias deste modo", escopada ao fluxo do EDE Legal Plugin
  (Skill genérica, usada por outras peças ainda não autorizadas —
  INV-GATE-CONTESTACAO; a regra geral da Skill não foi reescrita).
- `skills/humanizer-pt-br/SKILL.md`: nova nota logo após §13 ("Uso
  excessivo de travessão"), escopada ao fluxo do EDE Legal Plugin —
  vira proibição absoluta (não "reduza a frequência") só quando
  humanizando placeholders da Contestação; a regra geral de §13
  (reduzir uso excessivo, válida para qualquer texto) permanece
  inalterada, já que a Skill é transversal ao marketplace, não
  exclusiva do EDE.
- `skills/contestacao/SKILL.md`: nova subseção `INV-CONTESTACAO-SEM-
  TRAVESSAO` em §8, documentando a regra e o backstop no nível de
  orquestração.

## Backstop determinístico

`scripts/validate_placeholder_semantics.py`: `_checar_travessao(valor,
nome_campo)` roda sobre TODO placeholder em `dados` (não só os campos
com validador dedicado) dentro de `validar_semantica()` — universal, não
um backstop lexical pontual por campo como os demais desta camada.
Nunca substitui automaticamente o travessão por vírgula/hífen (a
pontuação correta depende da estrutura sintática da frase); só rejeita.
Integrado ao `stage=placeholders` já existente em
`scripts/gerar_contestacao.py` (`_etapa_placeholders`) — roda antes de
`paragrafo_380` e de `template_engine`, mesmo ponto de fail-closed já
usado para os demais backstops semânticos (meta-proveniência, contrato
atômico da irregularidade etc.). Escopo: só `dados` (placeholder
values) — nunca o texto fixo institucional do template, que este módulo
não lê.

## Fonte determinística corrigida

`scripts/gerar_contestacao.py`, valor de fallback de
`TEMPESTIVIDADE_CASO` (usado quando o redator não fornece um próprio):
trocado o travessão literal do f-string por dois-pontos. Não é
"conteúdo gerado pela IA" em sentido estrito (é texto construído pelo
orquestrador Python), mas alcança o mesmo placeholder do DOCX final —
mantê-lo violaria o resultado exigido (nenhum placeholder do DOCX final
contém U+2014), então foi corrigido junto.

## Escopo da proibição

Só o conteúdo variável (o que o Template Engine substitui). Texto
institucional fixo do modelo oficial nunca é alterado por esta regra,
mesmo que eventualmente contenha travessão — o backstop lê apenas
`dados` (dict de placeholders), nunca `word/document.xml` do template.

# 51. INV-JUIZO-DATAJUD

## Regra

`{{JUIZO}}` deixa de ser conteúdo gerativo. O endereço judicial da
Contestação deve ser obtido de fonte processual estruturada — a API
Pública DataJud/CNJ — e jamais inventado, inferido ou parafraseado pela
IA. A geração somente prossegue quando houver identificação segura do
órgão julgador; qualquer incerteza aborta o pipeline (fail closed),
nunca presume.

## Fluxo

```text
NUMERO_PROCESSO
  ↓ normalizar (20 dígitos, sem pontuação)
  ↓ identificar tribunal (segmento + TR do número CNJ) -> alias DataJud
  ↓ consultar DataJud (POST /<alias>/_search, match numeroProcesso)
  ↓ extrair _source.orgaoJulgador.{nome, codigo, codigoMunicipioIBGE}
  ↓ resolver comarca via IBGE (codigoMunicipioIBGE -> nome do município)
  ↓ montar "AO JUÍZO DA <orgaoJulgador.nome> DA COMARCA DE <comarca>"
{{JUIZO}}
```

Implementado em `scripts/datajud_client.py`, módulo isolado — o Template
Engine (`docx_template_engine.py`/`docx_block_engine.py`) não conhece
HTTP/DataJud, só recebe o valor já resolvido em `dados["JUIZO"]`.
Integrado à orquestração em `scripts/gerar_contestacao.py`
(`_etapa_juizo`, dentro de `_etapa_placeholders`, `stage=juizo_datajud`)
— roda antes do gate `paragrafo_380` e do motor documental.

## Documentação consultada antes de implementar (não presumida)

- https://datajud-wiki.cnj.jus.br/api-publica/endpoints/ — lista real de
  aliases por tribunal (usada para derivar `api_publica_tj<uf>`, com
  `api_publica_tjdft` como única exceção entre os 27 estados).
- https://datajud-wiki.cnj.jus.br/api-publica/exemplos/exemplo1/ —
  exemplo real de request (`{"query": {"match": {"numeroProcesso":
  "<20 dígitos>"}}}`) e response (confirma os campos
  `_source.tribunal`, `_source.orgaoJulgador.{nome, codigo,
  codigoMunicipioIBGE}`).
- https://datajud-wiki.cnj.jus.br/api-publica/acesso/ — autenticação via
  header `Authorization: APIKey <chave pública>`.
- Consulta real ao TJBA (processo 8000099-11.2026.8.05.0080, o mesmo
  caso usado nos testes reais deste projeto) confirmou o formato em
  produção: `orgaoJulgador.nome` = "2ª VARA DE FEITOS DE REL DE CONS.
  CÍVEL E COMERCIAIS" — não inclui a comarca, daí a resolução separada
  via IBGE (`codigoMunicipioIBGE` 2932903 -> "Valença", confirmado
  contra a API pública do IBGE, sem autenticação).
- CNJ Resolução nº 65/2008 — segmento 8 (Justiça Estadual): TR 01-27 na
  ordem alfabética dos nomes dos Estados/Distrito Federal. Tabela
  cruzada com os 27 aliases reais listados na página de endpoints
  (nenhum código "chutado" de memória sem essa checagem cruzada).

## Escopo suportado

Segmento do número CNJ: **8** (Justiça Estadual, 27 UFs — cobertura
completa, não restrita a TJBA), **4** (Justiça Federal, TRF1-TRF6),
**3** (STJ). Segmentos 5 (Trabalho), 6 (Eleitoral), 7/9 (Militar) **não
são suportados nesta implementação** — falham explicitamente
(`stage=juizo_datajud`) em vez de assumir uma convenção de TR não
verificada com a mesma checagem cruzada usada para os segmentos acima.
Extensão futura: adicionar ao mapeamento em `datajud_client.py` só
depois de repetir o mesmo processo de verificação documental.

## Fail-closed

`PIPELINE_ABORTED`, `stage=juizo_datajud`, nos cenários: DataJud
indisponível (timeout/erro de conexão após retries, HTTP 5xx
persistente); processo não encontrado (`hits.total.value == 0`);
múltiplos resultados com órgão julgador incompatível entre si; resposta
sem `orgaoJulgador.nome`; segmento/TR do número CNJ fora do escopo
suportado; `NUMERO_PROCESSO` ausente/malformado; `DATAJUD_API_KEY` não
configurada; `placeholders.json` contendo um `JUIZO` próprio (rejeitado
explicitamente, nunca usado nem reconciliado com a resolução
automática). Nenhum desses cenários é corrigido silenciosamente — a
substituição de travessão/hífen ou qualquer "melhor esforço" está
descartada por design.

## API key — versionada no repositório (Etapa 5.5, SUBSTITUI a decisão anterior)

A API Pública DataJud é autenticada por uma chave publicamente
documentada pelo DPJ/CNJ (`Authorization: APIKey <chave>`,
https://datajud-wiki.cnj.jus.br/api-publica/acesso/) — compartilhada,
não uma credencial pessoal secreta. Decisão expressa do projeto na
Etapa 5.5 **reverte** a orientação anterior (configuração externa
obrigatória): a chave passa a ser versionada no próprio repositório, num
único ponto canônico — `DATAJUD_API_KEY_PADRAO`, em
`scripts/datajud_client.py` — tornando o plugin autossuficiente para
elaboração em massa, sem exigir configuração manual do advogado.
Nenhum outro arquivo (Skill, fixture, teste, `plugin.json`,
`marketplace.json`) reproduz o valor; todo consumidor passa por
`resolver_juizo()`/`resolver_orgao_julgador()` (que já a resolvem) ou lê
a constante diretamente. `DATAJUD_API_KEY` (variável de ambiente)
permanece aceita como override — necessário se o CNJ rotacionar a chave
antes deste arquivo ser atualizado; nesse caso, uma tentativa com a
chave desatualizada retorna HTTP 401/403, tratado explicitamente como
`TAG_ERRO_AUTENTICACAO` (nunca retry, nunca degrada para JUIZO
gerativo, nunca busca/inventa nova chave sozinho — exige atualização
manual de `DATAJUD_API_KEY_PADRAO` ou definição do override). O secret
scan do projeto (`tests/test_pacote_distribuicao.py`) trata esta chave
especificamente como exceção expressa desde a Etapa 5.5 — testes A-F:
(A) chave existe no ponto canônico; (B) não duplicada em outro arquivo
rastreado; (C) `datajud_client.py` efetivamente resolve a partir dessa
constante; (D) ausência total de chave (canônica + override) continua
fail-closed; (E) erro de autenticação nunca degrada para um `JUIZO`
parcial; (F) nenhuma OUTRA credencial privada (AWS, PEM, tokens de API
genéricos) é versionada — o secret scan geral do projeto continua
íntegro para tudo que não for esta chave específica.

## Cache

`.cache/datajud/juizo_cache.json` (gitignored — mesmo tratamento de
dado sensível de processo dado a `rag/jurisprudencia/`, CLAUDE.md §18-19):
`numero_processo -> {tribunal, orgao_julgador_nome, orgao_julgador_
codigo, codigo_municipio_ibge, data_consulta}`, mais um namespace
`ibge:<codigo>` para o nome de município. Evita nova consulta para o
mesmo processo em elaboração em massa — sem TTL/expiração nesta versão
(órgão julgador raramente muda; se mudar, apagar a entrada manualmente
força nova consulta).

## Fonte de verdade — orgaoJulgador.nome nunca reinterpretado

O campo `orgaoJulgador.nome` retornado pelo DataJud constitui a fonte de
verdade da denominação da unidade judiciária. O plugin não deve expandir,
corrigir, reinterpretar ou substituir automaticamente abreviações ou a
nomenclatura oficial retornada pela fonte — `datajud_client.py` aplica
somente `.upper()` (padrão institucional de caixa alta já vigente para
todo o endereçamento da peça, mesma convenção de AUTOR/JUIZO desde a
Etapa 5.3), nunca uma reescrita de conteúdo.

## Limitação conhecida — comarca aproximada pelo município

A comarca é aproximada pelo nome do município resolvido via IBGE a
partir de `codigoMunicipioIBGE`. Correto para a grande maioria dos
casos (comarca = município-sede); não distingue comarcas que abrangem
mais de um município (situação rara na organização judiciária
brasileira, mas existente). Não corrigido nesta etapa — documentado
como limitação conhecida, não presumido silenciosamente como resolvido.

## Testes

`tests/test_datajud_client.py` (19 testes): normalização, identificação
de alias, resolução completa com mock de DataJud+IBGE, caixa alta
obrigatória, indisponibilidade/processo inexistente/resposta sem órgão
julgador/múltiplos resultados incompatíveis (fail-closed), cache evita
nova chamada, segmento não suportado (fail-closed), retry em falha
transitória vs. não-retry em HTTP 4xx, mais os dois testes da Etapa 5.5
(`test_sem_datajud_api_key_no_ambiente_usa_chave_canonica_automaticamente`,
`test_ausencia_total_de_chave_fail_closed`) e
`test_erro_de_autenticacao_marcado_e_nunca_degrada` (`codigo ==
TAG_ERRO_AUTENTICACAO`, nunca fallback silencioso). Nenhum teste depende
da disponibilidade real da API (mock de
`datajud_client._post_json`/`_get_json`). `tests/test_e2e_contestacao.py`:
teste J (IA não pode fornecer JUIZO arbitrariamente — rejeitado com
`stage=juizo_datajud`) + suíte completa reexecutada com um stub de JUIZO
injetado (`resolver_juizo_fn`), confirmando que as demais etapas
continuam funcionando. `tests/test_pacote_distribuicao.py` (Etapa 5.5,
testes A-F do §7 do prompt desta correção, substituem os 3 testes
`test_L_*` obsoletos que exigiam ausência da chave no repositório):
A. a chave existe no ponto canônico
(`datajud_client.DATAJUD_API_KEY_PADRAO`); B. não está duplicada em
nenhum outro arquivo rastreado; C. `datajud_client.py` usa o ponto
canônico (não uma cópia local); D. esvaziar o ponto canônico sem
`DATAJUD_API_KEY` no ambiente gera erro claro (fail-closed, nunca chave
inventada/buscada); E. erro de autenticação (`TAG_ERRO_AUTENTICACAO`)
nunca degrada para JUIZO gerativo; F. nenhuma outra credencial/secret
privado está versionada (secret scan preservado para tudo exceto a
chave pública DataJud, exceção expressa desta correção).

## Teste real de integração (controlado, fora da suíte automatizada)

Executado isoladamente (`python scripts/datajud_client.py
--numero-processo ...`), com a chave bundled (`DATAJUD_API_KEY_PADRAO`)
usada automaticamente sem configuração alguma — ver relatório da
correção para o resultado completo (processo real de teste do TJBA,
comarca de Valença — número interno, não versionado por CLAUDE.md §18-19;
~1s de tempo total incluindo DataJud + IBGE).

---

# 52. ETAPA 5.5 — TEMPESTIVIDADE NATURAL E NÃO-REPETIÇÃO FÁTICA

Correções decorrentes de um segundo Teste Real, sobre a Contestação já
com DataJud/`INV-CORTE-GATE-HUMANO` implementados (§51 documenta a
reconsolidação da chave DataJud; §45 documenta `INV-CORTE-GATE-HUMANO`
já revisado inline — esta seção cobre os dois achados que faltavam:
redação robótica de tempestividade e repetição do mesmo fato técnico
entre placeholders).

## INV-TEMPESTIVIDADE-PROCEDIMENTO-COMUM

Decisão de produto fixa e permanente: esta Contestação sempre calcula
tempestividade pelo procedimento comum do CPC — prazo de 15 dias, dias
úteis, art. 335 do CPC. Nunca a lógica do Juizado Especial (Lei
9.099/95), mesmo quando o caso concreto tramitar por um rito que
admitiria aplicação subsidiária dela. `scripts/gerar_contestacao.py`
(`_etapa_tempestividade`) aplica esse padrão fixo (`PRAZO_LEGAL_DIAS_
PADRAO = 15`, `TIPO_PRAZO_PADRAO = "uteis"`, `FUNDAMENTO_NORMATIVO_
PADRAO = "art. 335 do CPC (procedimento comum)"`) quando
`tempestividade.json` não especifica `prazo_legal_dias`/`tipo_prazo`/
`fundamento_normativo`; se algo incompatível for especificado (prazo
diferente de 15, tipo diferente de dias úteis, fundamento mencionando
Lei 9.099/95/Juizado Especial), o pipeline aborta (`stage=
tempestividade`) — nunca escolhe silenciosamente outro rito.

### Cálculo e redação são coisas diferentes (achado real)

O cálculo determinístico (`skills/calendario-forense-tjba-2026/scripts/
calcular_tempestividade.py`) continua produzindo uma memória de cálculo
completa (marco, termo inicial, dias úteis, suspensões, feriados, termo
final, status) — registrada integralmente no stage interno
`tempestividade` (`memoria_calculo()`), nunca descartada. O achado real:
essa estrutura estava sendo despejada quase literalmente no placeholder
`TEMPESTIVIDADE_CASO` (`"TEMPESTIVO: termo inicial 2026-08-29, prazo de
15 dias úteis (...), termo final 2026-09-21."`), lendo como relatório de
sistema, não como texto jurídico. Corrigido: `_redigir_tempestividade_
natural()` transforma o resultado em prosa jurídica curta e natural —
ex. `"A presente Contestação é tempestiva. Considerado o marco
processual ocorrido em 29/08/2026 e a contagem do prazo de 15 (quinze)
dias úteis, nos termos do art. 335 do Código de Processo Civil, o prazo
defensivo encerra-se em 21/09/2026, razão pela qual a defesa é
apresentada tempestivamente."` (datas sempre `DD/MM/AAAA`, nunca ISO;
ramo INTEMPESTIVO simétrico, declarando explicitamente a
intempestividade — a ausência desse branch explícito foi uma regressão
real detectada e corrigida na mesma correção, já que a versão anterior
ao menos prefixava `"INTEMPESTIVO:"`). Nunca menciona algoritmo, cálculo
interno, input, advogado, sistema, JSON, ou calendário como ferramenta —
só o resultado jurídico. `TEMPESTIVIDADE_CASO` continua só podendo ser
afirmado depois do cálculo efetivo (nunca "tempestiva" sem cálculo por
trás); resultado indeterminado continua fail-closed
(`r.status == PENDENTE` aborta antes de qualquer redação).

Backstop determinístico em `scripts/validate_placeholder_semantics.py`
(`_validar_tempestividade_caso`, não exaustivo): rejeita data em formato
ISO (`\d{4}-\d{2}-\d{2}`), marcadores de dump de memória de cálculo
(`"tempestivo:"`, `"intempestivo:"`, `"termo inicial"`, `"termo final"`),
e menção a Lei 9.099/95/Juizado Especial.

### Testes (`tests/test_tempestividade_redacao.py`, 18 testes) e demais

A-K conforme lista do prompt desta correção: prazo=15 dias (explícito
aceito, diferente rejeitado `stage=tempestividade`, ausente recebe
padrão); unidade=dias úteis (mesmo padrão); procedimento comum (padrão
aplicado quando ausente, compatível aceito quando explícito); cálculo
interno preserva memória completa (`memoria_calculo` com todos os
campos no stage); redação final não é dump da memória (sem
`prazo_legal_dias`/`fundamento_normativo`/`memoria_calculo` como texto);
sem prefixo `"TEMPESTIVO:"`/`"INTEMPESTIVO:"`; sem `"termo inicial"`/
`"termo final"` em formato de relatório; sem data ISO; sem menção a Lei
9.099/95 (aceito quando ausente, `fundamento_normativo` mencionando
9.099 aborta); redação é frase jurídica natural (tempestivo e
intempestivo, sem "advogado"/"sistema"/"json"/"algoritmo" no texto);
marco ausente continua fail-closed. `tests/test_e2e_contestacao.py`
(`test_happy_path_pipeline_completo`, `test_tempestividade_informada_
pelo_advogado`) atualizados: a asserção antiga
`r["tempestividade"].startswith("TEMPESTIVO")` (dump robótico) foi
substituída por `.startswith("A presente Contestação é tempestiva")` +
ausência de data ISO.

## INV-NAO-REPETICAO-FATICA

Achado do Teste Real: o mesmo fato técnico (ex. "desvio de energia antes
do medidor") reaparecia quase palavra por palavra em `REALIDADE_FATICA`,
na identificação da irregularidade e no desenvolvimento técnico —
produzindo sensação de texto gerado por IA sem edição. Princípio: **um
fato não deve ser reapresentado em vários parágrafos apenas com palavras
diferentes.** Cada placeholder narrativo tem função própria e exclusiva,
nenhum é uma "nova versão" do anterior:

- `SINOPSE_FATOS` — só a narrativa da inicial e os pedidos (contrato já
  existente, `INV-SINOPSE-ESTRITAMENTE-AUTORAL`).
- `REALIDADE_FATICA` — contraposição fática GLOBAL e objetiva da Ré (pode
  mencionar existência de inspeção, cobrança, suspensão, ausência de
  negativação, resultado geral do procedimento quando documentado) —
  mas NÃO antecipa o detalhe técnico que pertence aos subtópicos
  seguintes. Exemplo corrigido: `"A inspeção identificou irregularidade
  no sistema de medição, dando origem ao procedimento de recuperação de
  consumo documentado nos autos."`
- `IRREGULARIDADE_ENCONTRADA` — só o nome/tipo atômico da irregularidade
  (contrato já existente, Etapa 5.3-B, preservado sem alteração).
- `DESENVOLVIMENTO_TECNICO_IRREGULARIDADE` — explicação técnica
  complementar que ACRESCENTA informação nova (o mecanismo técnico
  concreto), nunca apenas reafirma o rótulo com outras palavras.
- `EVOLUCAO_CONSUMO` — só dado concreto de evolução de consumo, quando
  existente.
- `PEDIDOS_FINAIS` — conclusão processual, sem reencenar fatos.

Dado técnico concreto (código de irregularidade, número do TOI, data da
inspeção, critério de cálculo, período, ciclos, kWh, valor), quando
documentado, é distribuído racionalmente entre os campos — preferindo
um dado → uma função argumentativa → um local primário, não repetido em
múltiplos blocos.

**Presença da titular na inspeção**: mencionar a presença física da
titular na inspeção (quando documentalmente relevante) não pode ser
automaticamente transformada em conclusão jurídica de que o contraditório
foi "assegurado" — presença física ≠ conclusão jurídica de contraditório
garantido. Preserva-se a regra já existente (Etapa 5.3): a assinatura do
TOI nunca é usada como argumento defensivo.

Antes de finalizar um parágrafo narrativo, Redator/Humanizer verificam
se ele: (A) apresenta fato novo; (B) desenvolve consequência nova; (C)
realiza subsunção necessária; ou (D) apenas parafraseia informação já
dita noutro placeholder — só (D) exige remoção/reescrita.

### Classificação e backstop determinístico

O problema é SEMÂNTICO (paráfrase), não mera igualdade textual — não
foi criado bloqueador destrutivo baseado em igualdade/similaridade
textual genérica (geraria falsos positivos, vedado pelo prompt desta
correção). **Classificação: majoritariamente COMPORTAMENTAL** (Skills
`contestacao`/`redator-peca-processual-elite`/`humanizer-pt-br`), **com
um backstop determinístico NARROW**: `scripts/validate_placeholder_
semantics.py` (`_validar_nao_repeticao_realidade_fatica`, cross-field,
não exaustivo) rejeita quando o valor normalizado de
`IRREGULARIDADE_ENCONTRADA` (≥ 12 caracteres, para evitar falso positivo
em rótulos curtos) aparece verbatim como substring dentro de
`REALIDADE_FATICA` — o sintoma mais grave e detectável sem ambiguidade
do achado real, mesmo padrão de backstop lexical não exaustivo já usado
em `INV-SINOPSE-ESTRITAMENTE-AUTORAL`. Paráfrase (mesmo fato, palavras
diferentes) fica deliberadamente fora do escopo do código.

### Correção da fixture `happy_path`

`tests/fixtures/contestacao/happy_path/placeholders.json` e
`placeholders_redator.json` continham exatamente o padrão do achado real
(`REALIDADE_FATICA` repetindo "ligação direta no ramal de entrada"
verbatim de `IRREGULARIDADE_ENCONTRADA`) — corrigidos para a versão
abstrata/global, sem alterar `SINOPSE_FATOS`/`IRREGULARIDADE_ENCONTRADA`/
`DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`/demais campos.

### Testes (`tests/test_validate_placeholder_semantics.py`, 5 testes novos)

Rótulo atômico repetido verbatim em `REALIDADE_FATICA` → rejeitado
(`INV-NAO-REPETICAO-FATICA` na mensagem de erro). `REALIDADE_FATICA`
abstrata (exemplo corrigido) → aceita. Paráfrase com palavras diferentes
(mesmo fato) → aceita, deliberadamente fora do escopo do backstop.
`REALIDADE_FATICA` sem `IRREGULARIDADE_ENCONTRADA` no dict → não
bloqueada (nada a comparar). Rótulo curto (< 12 caracteres normalizados)
→ não gera falso positivo genérico.

# 53. INV-VALOR-DANO-MORAL-DOCUMENTAL — 13º placeholder

## Contexto

O advogado inseriu manualmente, no `modelo-oficial.docx` local, o
placeholder `{{VALOR_DANO_MORAL_PRETENDIDO}}` dentro do bloco condicional
já catalogado `DESCABIMENTO_DANO_MORAL` — auditoria direta do XML
(`word/document.xml`) confirmou a inserção antes de qualquer alteração de
código: exatamente uma ocorrência, fragmentada em 3 runs (`{{` /
`VALOR_DANO_MORAL_PRETENDIDO` / `}}`, cor `EE0000`), dentro da frase fixa
"A parte Autora pleiteou, de forma genérica e desprovida de
fundamentação específica, a condenação da Ré ao pagamento de indenização
por danos morais no valor de {{VALOR_DANO_MORAL_PRETENDIDO}}, contudo, a
mera formulação de pretensão indenizatória não se revela suficiente para
a procedência do pleito." **Esta correção não editou o template** — a
inserção já estava feita quando a auditoria começou (mesmo padrão de
§49); o trabalho aqui foi só de schema/código/documentação, acompanhando
essa mudança.

Divergência inicial resolvida por auditoria direta (não por confiança em
alegação): a primeira tentativa de invocar a correção afirmou que o
placeholder já existia, mas a auditoria do XML real não confirmou —
o arquivo havia sido editado no Word sem ainda estar salvo (lock file
`~$delo-oficial.docx` presente). Só depois de confirmação e nova
auditoria, que encontrou o token, a correção prosseguiu — CLAUDE.md §17
(Fail Closed) aplicado à própria auditoria do template, não só à geração
do DOCX final.

Homônimo do `VALOR_DANO_MORAL` previsto na SPEC-0001 original e removido
na auditoria de Fase 3 (REQ-014) — mas semanticamente distinto: aquele
seria um placeholder de PEDIDO de dano moral (que este modelo, de
Contestação, nunca formula, por ser uma peça defensiva); este é o valor
que a AUTORA pediu na inicial, citado defensivamente dentro do próprio
tópico que argumenta pelo descabimento do dano moral.

## Contrato

`VALOR_DANO_MORAL_PRETENDIDO` representa exclusivamente o valor da
indenização por danos morais efetivamente pretendido pela parte AUTORA
na petição inicial — dado factual documental. Redator, Humanizer e
Estrategista não podem inventar, estimar, arredondar, ou substituir esse
valor, nem confundi-lo com valor da causa, dano material, multa,
astreintes, repetição de indébito, honorários, Reconvenção ou qualquer
outro pedido econômico distinto.

Só é exigido quando `DESCABIMENTO_DANO_MORAL` está `INCLUIR` (a autora
formulou pedido de dano moral, extraído estruturalmente dos pedidos —
regra de elegibilidade já existente, comportamental, `skills/
contestacao/SKILL.md` §4). Sem pedido de dano moral, o bloco permanece
`EXCLUIR` e o placeholder nunca é exigido nem aparece — mecanismo
automático, sem gate/regra adicional em `blocos.json` (ver "Mecanismo de
obrigatoriedade" abaixo).

Pedido sem quantificação ("em valor a ser arbitrado pelo Juízo") é
conteúdo legítimo e comum — diferente de `VALOR_FRA`, não pode virar
fail-closed obrigatório. Mas o valor nunca pode ser impresso como a
sentinela literal de ausência ("NÃO ESPECIFICADO"/"NÃO INFORMADO"); a
frase deve ser natural o bastante para completar "...no valor de ___,"
sem soar como dump de sistema. Valores monetários divergentes no mesmo
campo (inicial contraditória, ou extração insegura sobre qual valor é o
do dano moral) são fail-closed: nunca escolher automaticamente o maior,
menor ou último valor — extração deve ser confirmada com o advogado.
Formato monetário brasileiro obrigatório quando há quantia (`R$
10.000,00`; nunca `10000`, `R$10000`, `10.000` sem centavos, ou formato
americano `R$ 10,000.00`).

## Mecanismo de obrigatoriedade — sem gate novo em `blocos.json`

Diferente de `VALOR_FRA`/`RECONVENCAO` (que declaram uma entrada em
`dependencies` com `regra: "obrigatorio_nao_sentinela"` — documentação
que, na prática, não é lida por nenhum código; a obrigatoriedade real de
`VALOR_FRA` vem da lista global `PLACEHOLDERS_CRITICOS_NAO_SENTINELA` em
`scripts/gerar_contestacao.py`), `VALOR_DANO_MORAL_PRETENDIDO` **não**
precisa de gate/regra adicional nenhuma: como o placeholder vive
fisicamente dentro do próprio `<w:sdtContent>` de `DESCABIMENTO_DANO_
MORAL`, a composição de blocos (`scripts/docx_block_engine.py`,
`compor_blocos`) já resolve os dois casos automaticamente, ANTES da
substituição de placeholders:

- `EXCLUIR` → o `<w:sdt>` inteiro (com o placeholder dentro) é removido
  do XML antes de `docx_template_engine.validar_placeholders` rodar —
  o placeholder nunca é cobrado, nunca aparece.
- `INCLUIR` → o conteúdo é "unwrapped" (o token permanece no XML
  composto); `validar_placeholders` (REQ-034) já verifica que todo
  placeholder PRESENTE NA XML COMPOSTA tem dado fornecido
  ("placeholders do template sem dado fornecido") — se
  `VALOR_DANO_MORAL_PRETENDIDO` estiver ausente de `placeholders.json`
  neste caso, o pipeline aborta no estágio `template_engine`, sem
  precisar de nenhuma lista/regra nova.

Por isso `VALOR_DANO_MORAL_PRETENDIDO` **não** entra em
`PLACEHOLDERS_OBRIGATORIOS_NAO_VAZIOS` nem em
`PLACEHOLDERS_CRITICOS_NAO_SENTINELA` (`scripts/gerar_contestacao.py`) —
adicioná-lo a qualquer uma delas quebraria o caso comum (autora não pede
dano moral) ou proibiria indevidamente "a ser arbitrado pelo Juízo".

## O que mudou

- `templates/contestacao/modelo-oficial.docx`: **não alterado por esta
  correção** — o placeholder já havia sido inserido manualmente pelo
  advogado antes do início do trabalho de código.
- `templates/contestacao/schema.json`: `VALOR_DANO_MORAL_PRETENDIDO`
  adicionado a `editable_placeholders` e `placeholder_contracts` (13
  placeholders, era 12).
- `templates/contestacao/blocos.json`: `DESCABIMENTO_DANO_MORAL.
  placeholders` passa a `["VALOR_DANO_MORAL_PRETENDIDO"]` (antes `[]`);
  `dependencies` permanece `[]` (mecanismo automático, ver acima).
- `scripts/validate_placeholder_semantics.py`: novo
  `_validar_valor_dano_moral_pretendido` (formato monetário BR, aceita
  frases de "sem quantificação", rejeita múltiplos valores divergentes)
  + entrada em `VALIDADORES_ESPECIFICOS`.
- `scripts/gerar_contestacao.py`: comentário explicando por que o campo
  NÃO entra nas duas listas globais de obrigatoriedade (nenhuma mudança
  de lógica).
- Fixtures: `tests/fixtures/contestacao/happy_path/placeholders.json` e
  `placeholders_redator.json` ganharam `VALOR_DANO_MORAL_PRETENDIDO`
  (`"R$ 10.000,00"`) — o fixture já tinha `DESCABIMENTO_DANO_MORAL =
  INCLUIR`, então passou a exigir o dado.
- Testes: `tests/test_validate_placeholder_semantics.py` (6 testes
  novos: monetário aceito, sem quantificação aceito, sem dígito/
  indicação rejeitado, 4 formatos inválidos rejeitados, múltiplos
  valores divergentes rejeitado, mesmo valor repetido não é falso
  positivo); `tests/test_e2e_contestacao.py` (4 testes novos: ausência
  de pedido não exige o placeholder, inclusão sem valor aborta,
  "a ser arbitrado" aceito e renderizado, valores contraditórios
  aborta — todos contra o `.docx` real) + assertiva nova em
  `_validar_conteudo_sintetico` (valor aparece no DOCX final);
  `tests/test_blocos_contestacao.py` (1 teste novo: catálogo real
  reflete o novo `placeholders`); `tests/test_argumentacao_evolucao_
  consumo_removida.py` e `tests/test_docx_block_engine.py` e `tests/
  test_template_engine.py` (dicts fictícios que incluem TODOS os blocos
  ganharam o campo, senão `validar_placeholders` acusa dado ausente).
- Documentação: `CLAUDE.md` §14 (lista de placeholders + novo INV-
  VALOR-DANO-MORAL-DOCUMENTAL), `docs/specs/SPEC-0001.md` (REQ-014,
  REQ-015, este §53), `docs/adr/ADR-0009-...md` (nota sobre a contagem
  de placeholders), `skills/contestacao/SKILL.md` (tabela de mapeamento
  de origem + regra de elegibilidade de `DESCABIMENTO_DANO_MORAL`),
  `skills/estrategista-contestacao-ede/SKILL.md` (bullet de elegibilidade
  do dano moral).

## Escopo não tocado

Confirmado intocado nesta correção: os demais 12 placeholders, os demais
blocos condicionais do catálogo, RAG, DataJud, tempestividade, Template
Lock, `scripts/docx_template_engine.py`/`docx_block_engine.py` (nenhuma
mudança de código nos motores — só dados de schema/catálogo, exatamente
como o mecanismo de obrigatoriedade acima já previa).

---

# 54. ETAPA 5.6 — ORQUESTRAÇÃO E ENTREGA: A CONTESTAÇÃO DEVE ENTREGAR O DOCX

## Achado do Teste Real

O advogado solicitou a elaboração da Contestação. A execução realizou
corretamente a análise estratégica (`estrategista-contestacao-ede`) e
encerrou a resposta entregando essa análise ao advogado, informando que
os próximos passos seriam RAG, Redator e Humanizer. Comportamento
incorreto: o objetivo operacional do plugin é a produção da peça — o
advogado não deve conduzir manualmente as etapas internas do pipeline.

## Causa raiz (investigada antes de qualquer correção)

Auditoria do contrato entre `skills/contestacao/SKILL.md` (orquestradora)
e `skills/estrategista-contestacao-ede/SKILL.md` (estrategista) localizou
a origem exata:

- `skills/estrategista-contestacao-ede/SKILL.md` §9 ("Depois da análise",
  antes desta correção) instruía, incondicionalmente: *"Se o usuário
  pedir a peça pronta em seguida, entregue esta análise primeiro como
  artefato próprio e então sugira o próximo passo explicitamente..."* —
  sem distinguir se a Skill fora acionada diretamente pelo usuário ou
  pela orquestração de `contestacao`. Como Skills do Claude Code são
  arquivos de instrução para o mesmo agente de conversa contínua — não
  chamadas de função isoladas com contrato de retorno (mesma limitação já
  documentada em `scripts/gerar_contestacao.py`, "LIMITAÇÃO CONHECIDA") —
  essa instrução se aplicava também quando `estrategista-contestacao-ede`
  era acionada como etapa interna do fluxo de `contestacao`, produzindo
  exatamente a frase "próximo passo recomendado..." relatada no achado.
- `skills/contestacao/SKILL.md` não continha, antes desta correção,
  nenhuma seção definindo explicitamente o contrato de entrega final ao
  advogado — a análise estratégica (§4) era descrita como "insumo
  principal", mas nada impedia, no nível de instrução, que ela fosse
  repassada como resposta de encerramento.
- Agravante: `skills/contestacao/SKILL.md` §1 ainda descrevia o escopo
  como "Fase 6", afirmando que a skill "não executa aqui, contra um caso
  real, o fluxo fim-a-fim até o DOCX final" — texto desatualizado (a
  Fase 7, SPEC-0001, já está ativa sem gate pendente desde a
  reclassificação de `PEND-001` como `DEFERRED`, e os "Testes Reais" das
  Etapas 5.2-5.5 já são execuções ponta a ponta) que reforçava, na
  própria auto-descrição da skill, a impressão de que parar antes do DOCX
  era o comportamento esperado.

Não havia bug de código — as duas correções são de instrução (Markdown
consumido pelo agente), não de `scripts/*.py`.

## Correção

1. `skills/estrategista-contestacao-ede/SKILL.md` §9 passa a distinguir
   os dois contextos de invocação: acionada pela orquestração de
   `contestacao`, a análise é insumo interno — a skill entrega a
   estrutura da seção 7 e para, sem "próximo passo"; acionada diretamente
   pelo usuário (fora do fluxo de `contestacao`), continua entregando a
   análise como artefato final com sugestão explícita de próximo passo,
   comportamento anterior preservado para esse caso.
2. `skills/contestacao/SKILL.md` ganha:
   - reforço em §4: a análise estratégica não é a resposta ao advogado
     quando a orquestração está em curso;
   - correção de §1 (Fase 7 já ativa, execução ponta a ponta já é o
     comportamento esperado, não um estado futuro);
   - passo 10 explícito no diagrama do pipeline (§3): entrega ao
     advogado;
   - nova §11 (`INV-CONTESTACAO-ENTREGA-DOCX`): modos de operação
     (PRODUÇÃO/ANÁLISE/CONSULTIVO/TESTE), proibição de checkpoint por
     etapa interna em modo produção, as duas únicas hipóteses normais de
     interrupção (decisão humana obrigatória pendente; fail-closed
     técnico/fático real), e o formato da resposta final (DOCX + nota
     breve, nunca despejo de artefatos internos).
3. `CLAUDE.md` §7 ganha a invariante `INV-CONTESTACAO-ENTREGA-DOCX`
   (resumo operacional, remete a este §54 e às seções correspondentes
   dos dois `SKILL.md`).
4. Novo teste de governança,
   `tests/test_orquestracao_entrega_contestacao.py` — mesmo padrão sem
   framework de `tests/test_contestacao_skill_dependencies.py` e
   `tests/test_gate_contestacao.py`: asserts estruturais sobre o
   CONTEÚDO de `CLAUDE.md`, deste `SPEC-0001.md` e dos dois `SKILL.md`
   envolvidos, verificando que a invariante está registrada, que a
   proibição de frases de "próximo passo" em modo produção está escrita,
   que os modos de operação estão distinguidos, e que o contrato revisado
   de `estrategista-contestacao-ede` §9 diferencia acionamento pela
   orquestração de acionamento direto.

## Por que só instrução, e não também código

Diferente das correções de placeholder/bloco (§§46-53), este achado não
tem componente determinístico verificável em runtime: o comportamento
"parar e entregar análise" ou "continuar até o DOCX" é decidido pelo
agente ao seguir as instruções das Skills durante a conversa — não há,
hoje, execução Python do pipeline completo fora de teste real assistido
(mesma limitação de `scripts/gerar_contestacao.py`, seção "LIMITAÇÃO
CONHECIDA"). Por isso a correção real está inteiramente nos `SKILL.md` —
os testes desta Etapa 5.6 são guardas estruturais sobre o CONTEÚDO desses
arquivos (mesmo padrão de `tests/test_contestacao_skill_dependencies.py`
e `tests/test_gate_contestacao.py`), não simulação de execução do
pipeline.

## Escopo não tocado

Confirmado intocado nesta correção: redação, template, placeholders,
blocos condicionais (`templates/contestacao/blocos.json` — só a leitura
do catálogo pelas seções revisadas, sem mudança de dado), RAG,
tempestividade, DataJud, limite de 380 caracteres, regras de dano moral,
regras de corte, regras de Reconvenção — nenhum `scripts/*.py` foi
alterado por esta correção.

---

# 55. INV-NUMERACAO-DINAMICA-CONTESTACAO — RENUMERAÇÃO DINÂMICA DOS TÓPICOS

## Contexto

Achado relatado diretamente pelo usuário (não um "Teste Real" numerado
das etapas anteriores): a Contestação usa numeração de tópicos/subtópicos
("1. TEMPESTIVIDADE", "3.1 – LEGALIDADE DOS PROCEDIMENTOS...", "3.1.1 –
SINOPSE DOS FATOS:"), mas parte dela é texto fixo digitado no template —
excluir um bloco condicional de nível superior (ex.: `PRELIMINARES`) não
recalculava os prefixos numéricos dos subtítulos que vinham depois,
produzindo salto/inconsistência de numeração no DOCX final.

## Causa raiz (investigada antes de qualquer correção)

Auditoria estrutural direta do `modelo-oficial.docx` real (nunca por
suposição) encontrou dois mecanismos de numeração coexistindo:

* **Nível 1** (títulos-badge — TEMPESTIVIDADE, PRELIMINARES, MÉRITO,
  LICITUDE DA COBRANÇA E CORTE, NEXO CAUSAL INDEMONSTRADO, DESCABIMENTO
  DE DANO MORAL, ÔNUS PROBATÓRIO, RECONVENÇÃO, REQUERIMENTOS) usa lista
  numerada NATIVA do Word (`w:numPr`, `numId=17`/`ilvl=0`,
  `abstractNumId 6`, `numFmt="decimal"`, `lvlText="%1."`, `start=1`). O
  número nunca existe como texto no XML — é calculado pelo Word na
  abertura do documento, pela posição do parágrafo entre os demais itens
  da mesma lista. Cada badge está corretamente aninhado dentro do próprio
  `<w:sdt>` do bloco a que pertence; excluir o bloco já remove o badge
  inteiro, e o Word recalcula os números remanescentes sozinho, sem
  lacuna. **Esta camada já era dinâmica por construção** e não é tocada
  por esta correção.
* **Níveis 2 e 3** (subtítulos — "2.1", "2.2", "3.1", "3.1.1", "3.1.2",
  "3.2", "3.3", "3.4", "4.1", "8.1") são texto literal digitado no
  `<w:t>` no momento da autoria do template, sem `w:numPr` e sem
  mecanismo algum (nativo do Word ou do pipeline) capaz de recalculá-los.
  `scripts/docx_block_engine.py` (`compor_blocos`) só faz unwrap/remove
  estrutural do `<w:sdt>` (INV-COMPOSICAO-BLOCOS) e nunca toca texto de
  título; `scripts/docx_template_engine.py` (`substituir_placeholders`)
  só toca runs com `{{PLACEHOLDER}}`. Excluir um bloco de nível 1
  deslocava corretamente os badges (via Word) mas não os prefixos
  hardcoded dos subtítulos que dependiam dele — o salto relatado.

Achado adicional, confirmado só durante a implementação (não presumido):
`compor_blocos()` **sempre remove a tag `<w:sdt>`**, tanto em `EXCLUIR`
quanto em `INCLUIR` (unwrap — o conteúdo sobrevive, a tag não). Na árvore
já composta, portanto, nenhuma `w:tag` de bloco condicional sobrevive,
incluído ou excluído — a renumeração não pode localizar um título de
nível 2/3 pela `w:tag` do seu SDT (única fonte estrutural disponível
antes da composição); só resta o próprio conteúdo (número + substring de
título) já presente no documento pós-composição.

## Contrato

> A numeração exibida na Contestação é responsabilidade exclusiva do
> motor documental. Após a composição definitiva dos blocos e a remoção
> dos blocos `EXCLUIR`, todos os títulos sobreviventes devem apresentar
> numeração sequencial, crescente, hierarquicamente coerente e sem
> lacunas.

Regras:

* a ordem estrutural dos blocos (definida pelo catálogo/template) nunca
  é alterada pela renumeração — ela só recalcula números, nunca
  reordena teses;
* nível 1 continua sendo resolvido pela lista nativa do Word (`numId=17`)
  — nunca reescrito por este motor; usado só para CONTAR a posição atual
  de cada seção (necessária como prefixo dos filhos de nível 2);
* níveis literais subordinados (2 e 3) são recalculados por
  `scripts/docx_numeracao_engine.py`;
* Redator, Humanizer e demais Skills de conteúdo nunca produzem,
  corrigem ou manipulam numeração — pertence exclusivamente ao motor
  estrutural;
* exclusão de bloco provoca renumeração automática dos sobreviventes,
  sem intervenção humana;
* containers excluídos (e seus descendentes) não deixam lacuna nem
  interferem na numeração dos irmãos seguintes;
* título numerado residual (de bloco removido), duplicidade, salto ou
  inconsistência hierárquica entre pai e filho causam fail-closed
  (`NumeracaoAbortada`/`ComposicaoAbortada`, nunca correção silenciosa);
* a renumeração ocorre depois da composição dos blocos e antes da
  substituição dos placeholders — nunca antes (a árvore ainda não estaria
  final) nem depois (colidiria com a formatação vermelha do conteúdo
  gerado);
* o Template Lock (REQ-016/TEST-001, ver REQ-002-B) permanece obrigatório
  e passa a cobrir também a renumeração — o "esperado" recomputado inclui
  a etapa de renumeração no mesmo caminho de código usado na geração real.

## Mecanismo (`scripts/docx_numeracao_engine.py`)

Motor novo, acionado por `docx_block_engine.gerar_peca_com_blocos()`
imediatamente após `compor_xml()` (composição), nos dois pontos em que
essa função já rodava (geração real e a recomputação do "esperado" do
Template Lock):

* nível 1: contado por localização 100% estrutural (`w:pPr/w:numPr`
  direto, `numId=17`/`ilvl=0`), nunca por texto — cada badge é desenhado
  duas vezes no template real (`mc:Choice` em DrawingML moderno +
  `mc:Fallback` em VML legado, mesma forma, dois formatos de
  compatibilidade); as duas cópias colapsam numa só pela adjacência do id
  já resolvido (nunca por identidade de objeto lxml — um proxy `_Element`
  efêmero pode ser reciclado pelo GC e colidir com outro nó; armadilha
  real encontrada e corrigida durante a implementação);
* nível 2/3: localizado pela combinação [prefixo numérico no início de
  uma run] + [substring-âncora única, auditada contra o template real, em
  algum lugar do parágrafo — não necessariamente na mesma run do número:
  achado real, em "2.1 - ", número e título vivem em runs distintas por
  causa de mudança de formatação]. Nunca é regex cego sobre o documento
  inteiro: cada âncora é específica de um único nó do catálogo interno de
  numeração, e qualquer ambiguidade (0 ocorrência num título obrigatório,
  ou mais de 1 ocorrência) aborta em vez de adivinhar;
* a reescrita substitui apenas o span inicial `\d+(\.\d+)*` do `<w:t>` já
  localizado — separador, espaço, título, `rPr`/`pPr`, runs vizinhas,
  bookmarks, SDTs, `mc:Choice`/`Fallback`, tudo permanece intocado;
* `validar_numeracao_final()` é um validador determinístico independente
  (mesmo padrão de `scripts/validate_placeholder_semantics.py`):
  reparseia o XML final, RECALCULA do zero o plano de numeração esperado
  a partir da árvore estrutural, e confere que bate exatamente com o que
  foi escrito — defesa em profundidade contra drift entre planejamento e
  escrita, não apenas confiança na construção determinística do próprio
  motor.

## O que mudou

* `scripts/docx_numeracao_engine.py` (novo) — motor de renumeração
  (`renumerar_titulos`, `validar_numeracao_final`, catálogo interno de
  nós de nível 1/2/3 auditado contra o template real).
* `scripts/docx_block_engine.py` — importa e aciona o novo motor logo
  após `compor_xml()`, nos dois pontos de composição (`transformar()` do
  Template Lock e o caminho principal de `gerar_peca_com_blocos`);
  `NumeracaoAbortada` tratada junto de `ComposicaoAbortada`; relatório
  final ganha a chave `"numeracao"` (auditoria).
* `scripts/gerar_contestacao.py` — repassa `"numeracao"` no stage
  `template_engine` do relatório operacional (REQ-035).
* `tests/test_docx_numeracao_engine.py` (novo) — testes de unidade sobre
  XML sintético pós-composição (sequência crescente, exclusão de tópico
  de nível 1, exclusão de filho intermediário sem lacuna, exclusão de
  vários filhos, container excluído sem interferência, container
  incluído em posição correta, Reconvenção e corte/suspensão
  incluídos/excluídos com numeração contínua, combinações sem duplicata,
  hierarquia pai/filho, preservação de texto/formatação OOXML, fail
  closed em título fixo ausente/badge desconhecido/numeração adulterada)
  e um teste `LOCAL_ONLY` ponta a ponta contra o `modelo-oficial.docx`
  real (Template Lock OK, DOCX final gerado com números corretos após
  excluir simultaneamente uma preliminar e o bloco de corte).
* `skills/contestacao/SKILL.md` §3 (diagrama do pipeline) — passo 9 passa
  a citar a etapa de renumeração entre a composição de blocos e o Template
  Engine, refletindo a sequência real de chamadas.
* `CLAUDE.md` §7 — nova invariante `INV-NUMERACAO-DINAMICA-CONTESTACAO`
  (resumo operacional, remete a este §55).

## Escopo não tocado

Confirmado intocado nesta correção: conteúdo jurídico, redação (Redator/
Humanizer nunca produzem ou corrigem numeração), `modelo-oficial.docx`
(nenhuma edição do asset — a numeração de nível 1 continua vindo
exclusivamente da lista nativa do Word já existente no template),
`templates/contestacao/blocos.json` (catálogo de composição não ganhou
campo novo — o catálogo de numeração é interno a
`docx_numeracao_engine.py`, conceito distinto), os 13 placeholders,
DataJud, tempestividade, RAG, regras de Reconvenção/corte/suspensão/dano
moral, `estrategista-contestacao-ede`.

---

# 56. INV-MODELO-INSTITUCIONAL-FONTE-PRIMARIA — CONTEXTO INSTITUCIONAL PARA O REDATOR

## Contexto

Achado da auditoria somente-leitura da Etapa 5.7-A (`docs/PENDENCIAS.md`
não aplicável — não é bug de Teste Real, é diagnóstico solicitado
diretamente pelo usuário antes de qualquer correção): o modelo oficial
(`templates/contestacao/modelo-oficial.docx`) contém, ao redor de cada um
dos 13 placeholders, texto institucional fixo que já formula parte do que
a peça precisa dizer (fundamentação, transição, qualificação). O Template
Lock (REQ-016) já garante que esse texto nunca é alterado — mas nada
garantia que o Redator (`redator-peca-processual-elite`) *soubesse* o que
esse texto já diz antes de redigir o valor de cada placeholder. Na
prática, o único mecanismo existente era a transcrição manual, pontual e
reativa a bugs reais, de trechos desse texto para dentro de
`skills/contestacao/SKILL.md` (ex.: a frase fixa ao redor de
`IRREGULARIDADE_ENCONTRADA`, corrigida na Etapa 5.3-B após duplicação
confirmada em teste) — cobrindo só 2 dos 13 placeholders, sujeito a ficar
desatualizado se o template mudar, e não reprodutível para os demais 11.

## Hipótese verificada (Etapa 5.7-A, confirmada com evidência de código)

"O modelo contém texto institucional, mas o Redator recebe apenas a
função do placeholder e redige conteúdo novo sem considerar
suficientemente o texto-base existente." Confirmada por leitura direta de
`scripts/gerar_contestacao.py` (nunca abre `modelo-oficial.docx` antes da
etapa final de geração), `skills/contestacao/SKILL.md` (só 2-3
placeholders com texto fixo citado literalmente) e
`skills/redator-peca-processual-elite/SKILL.md`/`skills/humanizer-pt-br/
SKILL.md` (nenhuma referência ao texto institucional fora dos casos já
remendados).

## Contrato

> Antes de produzir conteúdo para qualquer placeholder gerativo, o
> Redator deve conhecer o contexto institucional real existente no
> modelo oficial imediatamente relacionado àquele placeholder. O
> contexto é derivado diretamente do `modelo-oficial.docx` — nunca de
> transcrição manual em `SKILL.md` como mecanismo principal (transcrição
> manual deixa de ser necessária, não só desencorajada).

Regras:

* **modelo institucional = fonte primária da redação.** Ordem de
  princípio: PRESERVAR (texto fixo nunca muda) > COMPLEMENTAR (o
  placeholder acrescenta o que falta) > CRIAR (só dentro do placeholder,
  nunca fora dele). "ADAPTAR" aplica-se exclusivamente ao conteúdo
  produzido dentro dos placeholders autorizados — o texto institucional
  fixo nunca é adaptado, reescrito, parafraseado, resumido, humanizado,
  modernizado ou complementado diretamente pela IA;
* **`SEM PLACEHOLDER → SEM ESCRITA GERATIVA`** — os 13 placeholders
  oficiais (`templates/contestacao/schema.json`,
  `editable_placeholders`) continuam sendo as únicas zonas de
  intervenção textual da IA. Esta correção não cria zona de
  complementação nova, não altera catálogo/localização/nome/finalidade
  de nenhum placeholder, não permite edição cirúrgica de texto fixo, e
  não flexibiliza o Template Lock (REQ-016) — ele já garantia
  mecanicamente esta regra (Etapa 5.7-A §8: "já é verdade hoje, com uma
  única exceção não-IA"); esta correção formaliza a garantia, não a cria.
  **Exceção expressa, já existente e inalterada:** a renumeração
  determinística de títulos de nível 2/3
  (`scripts/docx_numeracao_engine.py`) não é escrita gerativa — é
  aritmética sobre dígitos, sem conteúdo jurídico, regulada
  integralmente por `INV-NUMERACAO-DINAMICA-CONTESTACAO` (§55). Nenhuma
  outra exceção é criada por esta correção;
* **extração somente leitura, derivada do template real.**
  `scripts/docx_context_engine.py` (novo) extrai, para cada ocorrência
  física de cada placeholder no `modelo-oficial.docx`, um objeto de
  contexto: `titulo` (título/subtítulo mais próximo antes da ocorrência —
  badge de nível 1 via `w:numPr numId=17`, ou título literal de nível
  2/3 via o mesmo padrão numérico já auditado por
  `docx_numeracao_engine.py`), `antes`/`depois` (texto fixo do próprio
  parágrafo ao redor do token; quando o placeholder está isolado no
  próprio parágrafo — caso comum, 9 dos 13 — cai para o parágrafo
  vizinho não vazio mais próximo, em ordem física do documento) e
  `bloco` (id do catálogo do `<w:sdt>` ancestral mais próximo, via
  `w:sdtPr/w:tag` + `templates/contestacao/blocos.json`, ou `None` se o
  placeholder não vive dentro de bloco condicional algum). Nunca abre o
  `.docx` mestre em modo escrita (mesma disciplina INV-012 de
  `docx_template_engine.gerar_peca`: sempre copia para pasta temporária,
  desempacota a cópia, descarta ao final) e nunca serializa/reescreve o
  XML — função pura de leitura;
* **placeholder duplicado no template vira múltiplas ocorrências, nunca
  uma só.** `VALOR_FRA`, `IRREGULARIDADE_ENCONTRADA` e `NUMERO_PROCESSO`
  aparecem mais de uma vez no template real (achado da Etapa 5.7-A); o
  contexto é uma LISTA de ocorrências por placeholder (uma por posição
  física), não um objeto único — cada ocorrência pode viver em bloco
  condicional diferente (ex.: `IRREGULARIDADE_ENCONTRADA` aparece uma vez
  fora de qualquer bloco e outra vez dentro de `RECONVENCAO`);
* **o Redator recebe o contexto, nunca decide sobre ele por conta
  própria contra o catálogo.** A extração não valida SDT contra catálogo
  (já feito por `docx_block_engine.validar_sdts_contra_catalogo` no
  caminho de geração real) nem placeholder contra schema (já feito por
  `docx_template_engine.validar_placeholders`) — é puramente consultiva;
  tag de SDT fora do catálogo apenas resulta em `bloco: None` para
  aquele contexto, nunca um abort (o abort estrutural correspondente já
  existe alhures, não duplicado aqui);
* **raciocínio obrigatório do Redator**, antes de redigir cada
  placeholder, com o contexto institucional em mãos: (A) o texto fixo ao
  redor já contém esta afirmação? Não repetir. (B) o placeholder só
  precisa inserir o dado específico do caso? Inserir somente o dado. (C)
  é necessária contraposição/complementação factual? Redigir só o
  conteúdo novo necessário. (D) o argumento jurídico já está
  desenvolvido no texto fixo? Não reescrever a fundamentação — fazer
  subsunção, não reexposição normativa (reforça
  `INV-NAO-REDUNDANCIA-NORMATIVA`, §7 do CLAUDE.md). (E) o conteúdo
  pretendido pertence semanticamente a outro placeholder? Não inserir
  (reforça `INV-NAO-REPETICAO-FATICA`, §52);
* **Humanizer nunca recebe texto institucional como conteúdo a
  reescrever.** O contexto institucional é insumo exclusivo do Redator,
  para decidir O QUE redigir dentro do placeholder — não é repassado ao
  `humanizer-pt-br` (que só recebe o texto já produzido pelo Redator para
  os placeholders, exatamente como hoje). Se algum fluxo futuro precisar
  disponibilizar contexto institucional também ao Humanizer, ele deve vir
  explicitamente marcado como **CONTEXTO NÃO EDITÁVEL** — o Humanizer
  jamais altera texto-base, mesmo que o veja;
* **cor (`FF0000`) e demais mecanismos de geração permanecem
  inalterados.** Texto institucional mantém a formatação original; só o
  conteúdo produzido dentro dos placeholders recebe `FF0000`
  (`docx_template_engine.py`, não tocado por esta correção);
* **placeholders com origem determinística continuam determinísticos.**
  `JUIZO` continua resolvido por `scripts/datajud_client.py`
  (`INV-JUIZO-DATAJUD`, §51), `FOTOS_DA_IRREGULARIADE` continua
  hardcoded por `scripts/gerar_contestacao.py` (`MARCADOR_FOTOS`),
  `NUMERO_PROCESSO`/`AUTOR`/valores continuam extração documental — o
  contexto institucional é relevante e consumido apenas pelos
  placeholders efetivamente GERATIVOS (redigidos pela IA); esta correção
  não introduz IA onde hoje existe resolução determinística.

## Mecanismo (`scripts/docx_context_engine.py`)

Módulo novo, sem alteração de nenhum motor existente:

* `extrair_contexto(document_xml: str, catalogo: dict) -> dict` — função
  pura (string + catálogo já carregado -> dict), nunca serializa a árvore
  lxml de volta;
* `extrair_contexto_do_template(template_path, catalogo_path) -> dict` —
  wrapper que copia o `.docx` mestre para pasta temporária, desempacota
  (reaproveita `docx_template_engine._importar_toolkit`, mesmo toolkit
  "docx" já usado por todo o motor documental — não duplicado), lê
  `word/document.xml` e chama `extrair_contexto`; a pasta temporária é
  sempre descartada, inclusive em erro;
* `ContextoAbortada` — fail-closed com o mesmo contrato
  (`stage`/`motivo`) de `ComposicaoAbortada`/`NumeracaoAbortada`, escopo
  estreito (só falhas de leitura: template ausente, unpack falhou, XML
  sem `<w:body>`);
* reaproveita, sem duplicar: `_PLACEHOLDER_RE` e `_importar_toolkit` de
  `docx_template_engine.py`, `carregar_catalogo`/`validar_catalogo` de
  `docx_block_engine.py`, e o mesmo critério de reconhecimento de título
  de nível 1 (`w:numPr numId=17`) e de nível 2/3 (prefixo numérico no
  início do parágrafo) já auditado por `docx_numeracao_engine.py` — como
  heurística de LEITURA (qual parágrafo "parece um título"), nunca como
  fonte de verdade estrutural para renumeração (essa continua sendo
  exclusivamente `docx_numeracao_engine.py`).

## O que mudou

* `scripts/docx_context_engine.py` (novo) — extração somente-leitura do
  contexto institucional por placeholder.
* `tests/test_docx_context_engine.py` (novo) — testes de unidade sobre
  XML sintético (antes/depois dentro do parágrafo e por fallback ao
  vizinho, título de nível 1/2/3, placeholder dentro de SDT, placeholder
  duplicado no mesmo bloco, `mc:Choice`/`Fallback` não gera contexto
  falso, garantia estrutural de que o módulo nunca serializa/grava XML) e
  testes `LOCAL_ONLY` contra o `modelo-oficial.docx` real (`VALOR_FRA`
  duplicado dentro de `RECONVENCAO`, hash do template mestre inalterado
  antes/depois da extração, os 13 placeholders batem exatamente com
  `schema.json`, E2E completo do contexto institucional).
* `skills/contestacao/SKILL.md` §9 — nova subseção descrevendo a etapa de
  extração de contexto institucional antes de acionar o Redator, e o
  raciocínio obrigatório (A-E) que o Redator aplica com esse contexto em
  mãos.
* `skills/redator-peca-processual-elite/SKILL.md` — nova regra, escopada
  ao fluxo do EDE Legal Plugin (mesmo padrão das demais regras
  Contestação-específicas já existentes nessa Skill transversal), sobre
  consumir o contexto institucional recebido e nunca reescrever/duplicar
  o texto fixo que ele descreve.
* `skills/humanizer-pt-br/SKILL.md` — nota explícita: contexto
  institucional, se disponibilizado, é sempre CONTEXTO NÃO EDITÁVEL;
  Humanizer nunca recebe texto-base como conteúdo a reescrever.
* `CLAUDE.md` §7 — nova invariante `INV-MODELO-INSTITUCIONAL-FONTE-
  PRIMARIA` (resumo operacional, remete a este §56) e a regra `SEM
  PLACEHOLDER → SEM ESCRITA GERATIVA`, com a exceção de
  `INV-NUMERACAO-DINAMICA-CONTESTACAO` expressa.

## Escopo não tocado

Confirmado intocado nesta correção: `modelo-oficial.docx` (nenhuma
edição do asset — extração é somente leitura, hash do arquivo mestre
verificado idêntico antes/depois em teste `LOCAL_ONLY`),
`docx_template_engine.py`, `docx_block_engine.py`,
`docx_numeracao_engine.py` (nenhum dos três motores existentes foi
alterado — o novo módulo só os importa/reaproveita), os 13 placeholders
(nome, quantidade, localização, condição de exigibilidade),
`templates/contestacao/blocos.json` (catálogo de composição não ganhou
campo novo), Template Lock, `decision_mode`s/gates/`state_linked`,
Reconvenção, licitude de corte/suspensão, gratuidade, DataJud,
tempestividade, RAG, jurisprudência, `estrategista-contestacao-ede`, dano
moral.

---

# 57. ETAPA 5.7-C — INTEGRAÇÃO OBRIGATÓRIA DO CONTEXTO INSTITUCIONAL AO FLUXO REAL

## Causa raiz

`scripts/docx_context_engine.py` (§56) foi entregue testado e correto,
mas **nenhum caminho de execução real o chamava** — só existia como
função disponível para ser invocada manualmente pela Skill `contestacao`
(instrução comportamental). O próprio relatório da Etapa 5.7-B já
sinalizava essa lacuna. Como Skills do Claude Code não são chamáveis de
Python (mesma limitação de sempre — `scripts/gerar_contestacao.py`,
"LIMITAÇÃO CONHECIDA"; mesmo padrão do achado de §54), não é possível
"impedir programaticamente" a invocação de `redator-peca-processual-
elite` sem contexto — mas é possível, e passa a ser feito, impedir que o
DOCX final seja produzido sem que a extração tenha rodado, com sucesso,
contra o template vigente, na mesma execução.

## Ponto de integração

`scripts/gerar_contestacao.py`, função `gerar()`: nova etapa
`_etapa_contexto_institucional()` inserida como **primeira etapa do
pipeline**, incondicional, antes até de `_etapa_fatos()`. Chama
`docx_context_engine.extrair_contexto_do_template()` diretamente contra
o `modelo-oficial.docx` do caminho `template` desta execução (nunca um
arquivo em cache/no diretório do caso) e filtra o resultado aos
placeholders efetivamente GERATIVOS (`schema.json`,
`placeholder_contracts.tipo` em `TEXTO_TECNICO`/`TEXTO_LONGO` — 6 dos 13:
`TEMPESTIVIDADE_CASO`, `SINOPSE_FATOS`, `REALIDADE_FATICA`,
`IRREGULARIDADE_ENCONTRADA`, `DESENVOLVIMENTO_TECNICO_IRREGULARIDADE`,
`PEDIDOS_FINAIS`) — placeholders determinísticos/documentais (`JUIZO`,
`FOTOS_DA_IRREGULARIADE`, `NUMERO_PROCESSO`, `AUTOR`, `VALOR_FRA`,
`VALOR_DANO_MORAL_PRETENDIDO`, `LOCAL_DATA`) nunca recebem contexto aqui
nem passam a ser gerados pela IA.

Falha nesta etapa (`ContextoAbortada`: template ausente, corrompido, ou
XML sem `<w:body>`) aborta o pipeline inteiro
(`PIPELINE_ABORTED`, `stage=contexto_institucional`) **antes** de
`_etapa_fatos`, `_etapa_estrategia`, `_etapa_placeholders` (que consome
`placeholders.json`, a saída do redator/humanizer) — nenhuma outra etapa
roda, nenhum documento do caso é sequer aberto. O relatório de sucesso
inclui `template_hash_sha256` (prova de que o contexto usado é o do
arquivo lido nesta mesma execução, nunca de transcrição manual ou cache)
e `placeholders_com_contexto`.

Achado adicional, corrigido durante a integração (não presumido):
`docx_context_engine.extrair_contexto_do_template()` checava apenas
`unpacked.exists()` após chamar o toolkit externo "docx" — insuficiente,
porque esse toolkit pode criar o diretório de saída mesmo quando o
`.docx` de entrada é inválido (zip corrompido/arquivo arbitrário),
deixando `word/document.xml` ausente e um `FileNotFoundError` não
tratado escapar (violação de fail-closed). Corrigido para checar
especificamente a existência de `word/document.xml` e abortar
(`ContextoAbortada("unpack_falhou", ...)`) de forma controlada.

## O que mudou

* `scripts/docx_context_engine.py` — `extrair_contexto_do_template()`
  passa a checar `word/document.xml` (não só o diretório) após o
  unpack, fail-closed em vez de crash não tratado.
* `scripts/gerar_contestacao.py` — nova constante `TIPOS_GERATIVOS`,
  nova função `_etapa_contexto_institucional()`, chamada como primeira
  etapa de `gerar()`; nova chave `"contexto_institucional": "OK"` no
  relatório de sucesso.
* `tests/test_docx_context_engine.py` — novo teste de `.docx`
  corrompido (fail-closed sem crash).
* `tests/test_integracao_contexto_institucional.py` (novo) — sequência
  obrigatória `contexto extraído → contexto disponível → drafting_
  humanization` (proxy code-level de "Redator autorizado") e o inverso
  (`contexto ausente/corrompido → pipeline aborta antes de qualquer
  outra etapa`); confirma que só os 6 placeholders gerativos recebem
  contexto; confirma que o hash reportado é do template lido nesta
  execução; confirma, pela própria assinatura da função, que a etapa não
  aceita (logo não pode ler) nenhum arquivo do diretório do caso.
* `tests/test_e2e_contestacao.py` — `test_fail_closed_template_
  institucional_ausente` reescrito: com o template ausente, o pipeline
  agora aborta na primeira etapa (`contexto_institucional`), antes de
  fatos/tempestividade/estratégia/RAG/redação — não mais só na etapa
  final do motor documental; `test_happy_path_pipeline_completo` passa a
  incluir `"contexto_institucional"` no conjunto de etapas esperado, como
  a primeira da lista.

## Escopo não tocado

Confirmado intocado: `modelo-oficial.docx` (hash idêntico antes/depois),
`docx_template_engine.py`, `docx_block_engine.py`,
`docx_numeracao_engine.py`, Template Lock, `FF0000`, os 13 placeholders,
composição de blocos, renumeração, DataJud, tempestividade, RAG,
Reconvenção, licitude de corte/suspensão, gratuidade, `humanizer-pt-br`
(continua recebendo só o texto já produzido pelo redator, nunca o
contexto institucional). Nenhum placeholder novo, nenhuma zona editável
nova, nenhuma flexibilização do Template Lock.
